services:
  web:
    build:
      context: .
    command: ["httpd", "-f", "-p", "8080", "-h", "/www"]
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "-", "http://127.0.0.1:8080/health.txt"]
      interval: 2s
      timeout: 1s
      retries: 10
