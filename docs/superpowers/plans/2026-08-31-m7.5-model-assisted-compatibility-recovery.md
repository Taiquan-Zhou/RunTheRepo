# M7.5 Model-Assisted Compatibility Recovery Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use
> `superpowers:subagent-driven-development` to implement this plan task-by-task.
> Every production behavior change also requires
> `superpowers:test-driven-development`; every unexpected result requires
> `superpowers:systematic-debugging`.

**Goal:** Restore real-repository compatibility without changing RepoTrial's
product identity: use a bounded local model only to propose typed Journey or
recovery data, preserve deterministic verification and KEEP/ROLLBACK, repair
the proven generic readiness/diagnostic gaps, calibrate disk requirements, and
then pass the frozen M7.5 cohort gates on one unified HEAD.

**Architecture:** Keep the existing LangGraph, `SandboxProvider`, RunState,
Journey DSL, verifier, experiment engine, and public report/API/CLI contracts.
Add private model/observer attempt evidence, correct the existing planner and
shared Boot implementation, and make any observer or disk correction only from
retained causal evidence.

**Tech Stack:** Python 3.12, Pydantic v2, LangGraph, pytest, existing
OpenAI-compatible model adapter, official Ollama on Windows, official
`qwen3:4b-instruct`, Docker Sandboxes v0.39.0 in the calibrated WSL2 Ubuntu
environment.

**Spec:**
`docs/superpowers/specs/2026-08-31-m7.5-model-assisted-compatibility-amendment.md`

## Preconditions and global constraints

- Worktree: `D:\A all code\RepoTrial-m7.5-compatibility-recovery`.
- Branch: `codex/m7.5-compatibility-recovery`.
- Planning base: `83ef63bcdb26b8ec3303881a78c0110b9c6ac1bc`.
- Accepted clean WSL baseline at that HEAD: `1256 passed, 5 skipped` using a
  pure Linux PATH containing `uv`, `git`, and standard system commands.
- Frozen manifest: `eval/real_repos.yaml`; do not edit it.
- Never run `sbx daemon start`, `stop`, `restart`, or `reset`.
- Never execute target Compose with host Docker or expose host Docker socket.
- PID hard bound remains unsupported and must be disclosed, not emulated.
- No repository-name conditionals, per-repository scripts, changed SHAs, skipped
  failures, weakened verifiers, or default success fallbacks.
- Do not modify LangGraph topology, `SandboxProvider`, RunState/checkpoints,
  Journey DSL/verifiers, experiment semantics, report/API/CLI semantics, or
  mutation architecture. Stop for Owner ruling if any becomes necessary.
- Each implementation task uses a fresh implementer and a different focused
  reviewer. The controller inspects every diff and reruns verification.
- Each task produces one scoped commit after review. No push or merge.

## Required verification commands

Run focused tests listed by each task, then all applicable gates before its
commit:

```text
uv run ruff check <changed Python paths>
uv run ruff format --check <changed Python paths>
uv run mypy src/repotrial
uv run pytest -q <focused tests>
uv run pytest --cov=src/repotrial --cov-branch --cov-report=term-missing --cov-fail-under=85
uv run pre-commit run --all-files
git diff --check
```

Before the metric canary, rerun the complete suite in WSL with the accepted
pure Linux PATH. Real SBX tasks must record lifecycle cleanup and verify exact
empty inventory afterward.

---

### Task 1: Freeze the amendment and calibrate the current local model runtime

**Files:**

- Add:
  `docs/superpowers/specs/2026-08-31-m7.5-model-assisted-compatibility-amendment.md`
- Add:
  `docs/superpowers/plans/2026-08-31-m7.5-model-assisted-compatibility-recovery.md`
- Add: `docs/dev/pilot-evidence/m7.5-model-runtime-calibration.md`

**Steps:**

- [ ] Record the clean planning HEAD, accepted WSL baseline, frozen manifest
      hash, and clean worktree.
- [ ] Check the installed Ollama version against the current official stable
      Windows release. If older, update with the official Windows installer in
      the default program location without deleting the existing model store.
- [ ] Do not add Ollama or Qwen to Python package dependencies.
- [ ] Confirm `qwen3:4b-instruct` exists locally and record its full digest,
      size, quantization, and Ollama version. Do not record credentials or proxy
      values.
- [ ] From the WSL execution environment, run the exact existing
      `RecoveryAction` and private `_JourneyProposal` schemas through the actual
      `OpenAICompatibleModelAdapter` against the local OpenAI-compatible
      endpoint.
- [ ] Record cold and two warm latencies, HTTP class, schema/fallback mode,
      validation result, and sanitized error metadata when rejected. Persist
      only allowlisted HTTP status and `error.type`/`error.code`/`error.param`,
      plus body byte length and SHA-256; never persist the raw error body or
      `error.message`.
- [ ] Run a no-model control proving current fail-closed behavior remains `[]`
      or recovery `stop`.
- [ ] Self-review the documents for conflicts with the frozen project plan,
      then obtain independent architecture/spec review before commit.

**Gate:** This task authorizes adapter fallback work only when the calibrated
current runtime reproduces a narrowly identifiable structured-schema
capability incompatibility. A generic 5xx, network error, auth failure, quota,
rate limit, or invalid model never authorizes fallback.

**Commit:**

```text
docs: approve model-assisted M7.5 recovery
```

---

### Task 2: Persist private model-planning attempt evidence

**Files:**

- Create: `src/repotrial/trial/model_evidence.py`
- Create: `tests/unit/trial/test_model_evidence.py`
- Create: `src/repotrial/trial/journey_artifact.py`
- Create: `tests/unit/trial/test_journey_artifact.py`
- Modify: `src/repotrial/trial/planner.py`
- Modify: `tests/unit/trial/test_journey_planner.py`
- Modify: `tests/unit/trial/test_recovery.py`
- Modify: `src/repotrial/agent/graph.py`
- Modify: `tests/unit/agent/test_graph.py`
- Modify: `src/repotrial/models/openai_compat.py`
- Modify: `tests/unit/models/test_openai_compat.py`

**Private interfaces:**

```python
type ModelAdapterFailureCode = Literal[
    "transport_error",
    "http_error",
    "response_too_large",
    "structured_response_invalid",
]

class ModelAdapterError(RuntimeError):
    reason_code: ModelAdapterFailureCode

type ModelAttemptOutcome = Literal[
    "success",
    "planner_timeout",
    "transport_error",
    "http_error",
    "response_too_large",
    "structured_response_invalid",
    "policy_rejected",
    "adapter_error",
]
```

`ModelAdapterError` keeps its existing credential-free public message, gains a
closed reason code, and stops inheriting `TimeoutError`; planner code catches it
explicitly. The `ModelAdapter` protocol itself is unchanged. Runtime/model
identity is frozen in the external execution ledger and is not introspected
from arbitrary adapters.

**RED tests:**

- [ ] A successful Journey proposal persists start/success metadata, elapsed
      monotonic time, schema/input/output hashes, and Journey count without raw
      README, logs, model output, endpoint query, or secrets.
- [ ] Timeout, transport/schema rejection, and policy rejection persist distinct
      bounded reason codes and preserve existing fail-closed return values.
- [ ] Recovery planning writes inside the claimed Boot attempt directory;
      baseline Journey planning writes inside the validated run evidence root.
- [ ] Existing `plan_journeys()` and `propose_recovery()` calls without an
      evidence target preserve behavior and signatures used by existing code.
- [ ] Existing/symlink destinations are never overwritten or followed;
      serialization/persistence failure is fail-closed.
- [ ] The clock is monotonic and elapsed values are finite/non-negative.
- [ ] Accepted Journeys are written before any target workload to a private,
      canonical, no-overwrite `baseline-journeys.json` containing every step,
      param, and assertion plus the Journey payload SHA-256.
- [ ] Baseline and candidate replay both fail closed before requests/browser
      actions if re-canonicalized RunState Journeys do not match the persisted
      artifact hash.

**GREEN implementation:**

- [ ] Add one small private evidence writer with strict enum-like values,
      canonical JSON, bounded text-free metadata, SHA-256 helpers, and atomic
      no-overwrite persistence.
- [ ] Add private planner entry points that delegate to the same planning logic
      as the existing functions while supplying a diagnostic destination.
- [ ] Wire Graph only after `_intake` has pinned the exact workspace. Do not add
      fields to GraphState, RunState, reports, or checkpoints.
- [ ] Persist the exact accepted normalized Journey JSON through existing
      RunState flow and the private canonical Journey artifact; model-attempt
      evidence stores only its hash/count. Do not change public report output.
- [ ] Add the closed `ModelAdapterError.reason_code` taxonomy above and replace
      its misleading `TimeoutError` inheritance. Do not parse exception text.

**Focused verification:**

```text
uv run pytest -q tests/unit/trial/test_model_evidence.py tests/unit/trial/test_journey_artifact.py tests/unit/trial/test_journey_planner.py tests/unit/trial/test_recovery.py tests/unit/models/test_openai_compat.py tests/unit/agent/test_graph.py
```

**Commit:**

```text
feat: retain bounded model planning evidence
```

---

### Task 3: Persist private observer boundary evidence

**Files:**

- Create: `src/repotrial/trial/observation_evidence.py`
- Create: `tests/unit/trial/test_observation_evidence.py`
- Modify: `src/repotrial/trial/observer.py`
- Modify: `tests/unit/trial/test_observer.py`
- Modify: `src/repotrial/agent/graph.py`
- Modify: `tests/unit/agent/test_graph.py`

**RED tests:**

- [ ] Each discovery, inspect, diff, top/process, and network collector emits one
      start event and exactly one success/failure event in deterministic order.
- [ ] Events distinguish provider execution, result validation, parsing,
      serialization, and atomic persistence phases with stable reason codes.
- [ ] Provider exceptions propagate unchanged while the private ledger retains
      only sanitized exception class and operation metadata.
- [ ] Nonzero exit, malformed `ExecResult`, parse failure, network unsupported,
      oversized evidence, and destination collision remain fail-closed and are
      distinguishable.
- [ ] Only argv hash/count and stdout/stderr length/hash/truncation metadata are
      persisted; raw output, environment values, and credentials are absent.
- [ ] Existing public `collect_observation()` behavior remains unchanged when no
      diagnostic path is supplied.

**GREEN implementation:**

- [ ] Add a bounded append-only JSONL writer using no-follow/no-overwrite-safe
      creation inside the validated attempt directory.
- [ ] Route Graph baseline observation through a private shared implementation
      with the diagnostic path. Keep hardening/candidate observation on the same
      collector implementation and semantics.
- [ ] Do not translate private reason codes into public report fields.

**Focused verification:**

```text
uv run pytest -q tests/unit/trial/test_observation_evidence.py tests/unit/trial/test_observer.py tests/unit/agent/test_graph.py tests/unit/hardening/test_engine.py
```

**Commit:**

```text
feat: retain observer boundary diagnostics
```

---

### Task 4: Correct model deadline and structured Journey compatibility

**Files:**

- Modify: `src/repotrial/trial/planner.py`
- Modify: `tests/unit/trial/test_journey_planner.py`
- Modify: `tests/unit/trial/test_recovery.py`
- Modify: `src/repotrial/models/openai_compat.py`
- Modify: `tests/unit/models/test_openai_compat.py`
- Update: `docs/dev/pilot-evidence/m7.5-model-runtime-calibration.md`

**RED tests:**

- [ ] A model response completing after 100 ms but within the calibrated planner
      timeout is accepted after existing validation.
- [ ] A cold local-model response completing after the former 10 second HTTP
      read timeout but within the calibrated 90 second request timeout is not
      terminated by the adapter.
- [ ] A response beyond the calibrated timeout is cancelled, observed, and
      fails closed without background-task leakage.
- [ ] Journey prompt describes only existing supported tools/actions/params and
      assertion kinds/targets; generated examples do not add DSL authority.
- [ ] Policy-invalid Journey values still return `[]`; invalid recovery actions
      still return `stop`.
- [ ] The current calibrated runtime's exact Journey schema succeeds directly;
      no new JSON-only fallback path is added. Existing 400/422 explicit
      capability fallback and all forbidden error classifications retain their
      current behavior.

**GREEN implementation:**

- [ ] Set the adapter read/request ceiling to 90 seconds while preserving its
      3 second connect timeout, and replace `_MODEL_TIMEOUT_S = 0.1` with a
      95 second planner ceiling. The measured official-runtime cold probe was
      78.872 seconds and warm Journey maximum was 8.123 seconds; the planner
      remains the outer bound, including any existing retry.
- [ ] Keep explicit task cancellation/exception observation.
- [ ] Strengthen the bounded prompt/schema description with exactly this
      existing DSL matrix, then reuse strict materialization and deterministic
      verification unchanged:
      - return at most 5 Journeys; each Journey uses one tool type and contains
        1-8 steps;
      - `journey_id`, `name`, and each `step_id` are non-empty, at most 4,096
        characters, and contain no Unicode category-C characters;
      - `http/request`: method `GET|POST|DELETE`, root-relative `path`, optional
        bounded JSON `json`; HTTP paths are ASCII, at most 2,048 characters,
        begin with one `/`, contain no fragment, backslash, dot segment, unsafe
        decoded segment, or unsafe query characters; assertions are
        `status_code` on
        `response.status` with integer expected, `text_contains` on
        `response.text` with text expected, or `json_path_equals` on a dotted
        path in the response JSON body with bounded JSON expected; an HTTP
        Journey has at least one assertion across its steps;
      - `browser/goto`: exactly `{path}` using the HTTP path restrictions plus
        no query string;
      - `browser/fill_by_label`: exactly `{label, value}`, where label is
        non-empty bounded ASCII and value is non-empty bounded text;
      - `browser/click_by_role`: exactly `{role, name}` where role is one of
        `button|link|checkbox|radio|menuitem|option|tab` and name is non-empty
        bounded ASCII;
      - `browser/assert_text_visible`: exactly `{text}` with non-empty bounded
        ASCII;
      - browser steps carry no separate assertion objects.
- [ ] Do not generalize any 500 response into capability fallback. The old
      0.20.7 schema 500 was not reproduced on calibrated 0.33.2 and authorizes
      no adapter fallback change.

The planner timeout is an independent bounded model-call limit. It does not
query `DockerSbxProvider._trial_deadline`, add a Provider method, or add a
checkpoint deadline. Existing Provider workload operations continue enforcing
their frozen create-started whole-trial deadline.

**Real calibration:**

- [ ] Repeat RecoveryAction and Journey probes three times through the actual
      adapter. Require policy-valid Journey output and stable fail-closed
      handling of an intentionally invalid response.
- [ ] Unload the calibrated model through the official model-runtime API only,
      then run one cold exact-schema call through the modified real adapter.
      It must return and validate inside the 95-second outer planner deadline;
      otherwise Task 4 and the metric-cohort gate fail. Do not infer this result
      from the native prewarm measurement.

**Focused verification:**

```text
uv run pytest -q tests/unit/trial/test_journey_planner.py tests/unit/trial/test_recovery.py tests/unit/models/test_openai_compat.py
```

**Commit:**

```text
fix: make bounded model planning usable
```

---

### Task 5: Correct shared Compose readiness and retire dead recovery wait

**Files:**

- Modify: `src/repotrial/trial/boot.py`
- Modify: `tests/unit/trial/test_boot.py`
- Modify: `src/repotrial/trial/planner.py`
- Modify: `tests/unit/trial/test_recovery.py`
- Modify: `src/repotrial/agent/graph.py`
- Modify: `tests/unit/agent/test_graph.py`
- Modify: `src/repotrial/agent/state.py`
- Update: `docs/dev/pilot-evidence/m7.5-compatibility-diagnostic.md`

**RED tests:**

- [ ] Baseline and candidate Boot both execute `up -d --wait --wait-timeout 60`
      through the shared `boot_compose()` implementation.
- [ ] `ps` and `logs` still run after `up`; exited services fail closed and
      health-aware readiness remains deterministic.
- [ ] Provider command timeout and whole-trial deadline still bound the wait;
      cleanup/destroy remains outside the exhausted trial deadline.
- [ ] The deterministic recovery planner no longer proposes `wait` for generic
      `starting` evidence.
- [ ] A model or checkpoint-compatible externally supplied `wait` action is
      rejected by the trusted executor and ends with the existing
      `boot_recovery_stopped` semantic.
- [ ] No host sleep occurs and the now-unused `GraphContext.sleep` hook is
      removed only after all callers/tests prove it dead.

**GREEN implementation:**

- [ ] Make the minimal shared argv correction in `boot_compose()`.
- [ ] Remove only the unreachable/dead host-side wait execution path. Preserve
      RecoveryAction schema compatibility and all other recovery actions.

**Real SBX smoke:**

- [ ] Using a trusted delayed-health fixture, prove create -> Compose wait -> ps
      -> logs -> exec echo -> destroy and exact empty final inventory. Do not
      issue daemon lifecycle commands.

**Focused verification:**

```text
uv run pytest -q tests/unit/trial/test_boot.py tests/unit/trial/test_recovery.py tests/unit/agent/test_graph.py tests/unit/hardening/test_engine.py
```

**Commit:**

```text
fix: use trusted Compose readiness
```

---

### Task 6: Diagnose and, if proven, correct one observer incompatibility

**Files:**

- Update: `docs/dev/pilot-evidence/m7.5-observer-diagnostic.md`
- Conditional modify: `src/repotrial/trial/observer.py`
- Conditional modify: `tests/unit/trial/test_observer.py`
- Conditional modify: `tests/integration/eval/test_fixture_benchmark.py`

**Diagnostic steps:**

- [ ] On the current reviewed HEAD and frozen changedetection.io SHA, run one
      `diagnostic-only` attempt with the model/runtime fingerprint frozen.
- [ ] Retain exact stop reason, private observer JSONL, lifecycle evidence,
      report disposition, and final `sbx list` inventory.
- [ ] Identify the exact collector, phase, exit/exception class, and normalized
      causal signature. Do not infer from `ObservationCollectionError` alone.

**Decision:**

- [ ] If evidence proves one generic Compose/observer compatibility defect,
      write the smallest fixture/regression test and observe RED before changing
      production code.
- [ ] Implement at most one generic correction and preserve unsupported/not
      observed semantics.
- [ ] If target-specific, unsupported, or still insufficient, commit only the
      diagnostic evidence and retain the canary failure. Do not patch.

**Focused verification when code changes:**

```text
uv run pytest -q tests/unit/trial/test_observer.py tests/integration/eval/test_fixture_benchmark.py tests/unit/hardening/test_engine.py
```

**Commit when code changes:**

```text
fix: handle proven observer compatibility case
```

**Commit when no code change is authorized:**

```text
docs: record observer compatibility diagnosis
```

---

### Task 7: Calibrate Umami disk minimum without weakening the hard bound

**Files:**

- Add: `docs/dev/pilot-evidence/m7.5-disk-capacity-calibration.md`
- Conditional modify: `src/repotrial/cli.py`
- Conditional modify: `src/repotrial/api/app.py`
- Conditional modify: `tests/unit/test_cli.py`
- Conditional modify: `tests/unit/api/test_app.py`
- Inspect only unless its mapping is independently disproven:
  `src/repotrial/sandbox/docker_sbx.py`
- Inspect only unless its mapping is independently disproven:
  `tests/unit/sandbox/test_docker_sbx_commands.py`

**C0/C1 steps:**

- [ ] Record current official root/Docker/workspace floors and the exact current
      2,048 MiB allocation.
- [ ] C0 uses this fixed same-total sequence and stops at first successful Boot:
      `(R,D,W)=(1,1919,128)` then `(1,1983,64)` MiB. The existing allocation
      `(1,1791,256)` is the retained control. Sixty-four MiB is the previously
      proven cloned-workspace compatible allocation; no lower value is tested.
- [ ] C1 runs only if C0 fails and uses this fixed total/allocation sequence:
      `3072 -> (1,2687,384)`, `4096 -> (1,3583,512)`, and
      `6144 -> (1,5375,768)` MiB. Stop at first successful Boot; 6,144 MiB is
      the diagnostic maximum and no value is chosen after seeing results.
- [ ] Every attempt uses exact Umami SHA, no host fallback, whole-trial deadline,
      forced cleanup, and exact empty final inventory. Do not fill disks
      destructively.
- [ ] Record first successful total/allocation or a bounded unsupported result.

**C2 decision:**

- [ ] Do not change the default policy from Umami evidence alone. Compare the
      proven minimum against the frozen cohort's declared workloads and prior
      retained failures.
- [ ] If a general default change is necessary, stop for an explicit Owner C2
      ruling before production edits. If approved, add a RED test for the new
      default and change only the policy default/test/documentation; leave disk
      mapping unchanged.

**Commit:**

```text
docs: calibrate M7.5 disk capacity
```

If a separately approved C2 code change occurs, use a separate commit:

```text
fix: raise bounded sandbox disk default
```

---

### Task 8: Freeze one execution HEAD and run the metric cohort

**Files:**

- Update: `docs/dev/pilot-report.md`
- Update: `docs/dev/status.md`
- Update only after successful gate: `README.md`
- Add/update append-only attempt evidence under the existing ignored artifact
  and audit-ledger locations; never commit secrets or raw credentials.

**Pre-canary gate:**

- [ ] Independent final code review reports no open Critical or Important
      findings for Tasks 2-7.
- [ ] Controller reviews the complete base-to-HEAD diff and verifies no manifest,
      architecture, verifier, experiment-semantic, or PID-claim drift.
- [ ] Ruff lint/format, strict mypy, focused suites, full pytest, coverage at
      least 85%, pre-commit, `git diff --check`, ancestry, and clean status pass.
- [ ] Real trusted SBX smoke passes and final inventory is empty.
- [ ] Freeze RepoTrial HEAD, manifest SHA-256, Ollama version, full model digest,
      endpoint class, schema/fallback mode, timeout, and prompt/schema hashes.
- [ ] Before freezing the metric fingerprint, perform one trusted bounded
      native Ollama prewarm with no repository/log input, then verify the exact
      model digest is resident. Task 4's separate cold exact-schema adapter gate
      must already have passed; prewarm only removes evaluation variance.
- [ ] From the first metric attempt until repository #10 completes or the
      canary stops, do not edit or commit any tracked file, including
      documentation. Before and after every repository, and again after the
      canary and complete cohort, re-check exact HEAD, exact empty output from
      `git status --porcelain=v1 --untracked-files=all`, manifest SHA-256,
      Ollama version, full model digest, endpoint class, structured-schema or
      bounded-fallback mode, timeout, and prompt/schema hashes. A mismatch
      invalidates the current metric cohort and preserves its attempts as
      superseded evidence.

**Metric execution:**

- [ ] Run Umami, Listmonk, and changedetection.io sequentially from the frozen
      manifest with exact SHAs and the frozen local model/runtime.
- [ ] A repository passes only with exact SHA, non-empty persisted required
      Journey set, every required Journey deterministically PASS, completed
      report, exact stop reason evidence, successful cleanup, and empty final
      inventory.
- [ ] If fewer than `2/3` pass, retain all attempts, update the report, and stop.
      Do not patch or rerun in place.
- [ ] If at least `2/3` pass, continue repositories 4-10 without production,
      test, manifest, model, runtime, or prompt changes.
- [ ] Compute frozen metrics: autonomous success, meaningful convergence, exact
      stop reason, cleanup, latency distribution, failure classes, and Journey
      hashes. Historical/superseded/diagnostic attempts remain audit-only.

**Documentation closeout:**

- [ ] Update README only from observed final-cohort facts. Document optional
      Ollama/Qwen setup, model/runtime fingerprinting, no-model fail-closed
      behavior, WSL2/SBX prerequisites, and PID hard-bound limitation.
- [ ] Do not claim globally safe, globally least privilege, or universal Compose
      compatibility.
- [ ] Obtain independent report/evidence review and controller verification.

**Final commit:**

```text
docs: record M7.5 real-repository pilot
```

## Stop conditions

Stop and request Owner ruling only when:

- a frozen architecture or public contract must change;
- the manifest, SHA, or repository-specific behavior would need modification;
- host fallback, credential exposure, cleanup failure, or non-empty inventory is
  found;
- SBX daemon lifecycle action is required;
- Task 6 would require a second unrelated observer correction;
- C2 requires a default disk-policy change;
- the final canary fails or the complete cohort finishes.
