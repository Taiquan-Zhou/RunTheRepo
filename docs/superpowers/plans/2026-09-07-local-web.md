# Local Web Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Preserve RED/GREEN evidence and perform one concentrated module review.

**Goal:** Expose the existing CLI trial through a usable loopback-only console.

**Architecture:** A local FastAPI page delegates to a single-flight CLI subprocess
runner and reads only bounded, owned terminal artifacts. Existing graph/provider
and PostgreSQL API behavior stays unchanged.

**Tech Stack:** Existing Python 3.12, FastAPI, Jinja2, uvicorn, pytest, Playwright.

**Spec:** `docs/superpowers/specs/2026-09-07-local-web-design.md`

## Global Constraints

- Authoritative linked WSL worktree remains `/home/repotrial/src/RepoTrial-m7.5-release-gap`;
  BASE_SHA is `16794fd459f4e57fe106c8939ebae84d2955da20`.
- Preserve the two pre-existing image-template plan/spec edits.
- No new dependencies, host target execution, daemon changes, API-key output,
  public bind, real sandbox parallelism, weakened gates, or automatic release.
- Owner routing: Luna High implementation, Sol High review, controller final gate.

## Task 1: Local console (one integrated deliverable)

Files: add `src/repotrial/__main__.py`, `src/repotrial/local_web/{__init__,app,runner}.py`,
`src/repotrial/local_web/templates/index.html.j2`, and focused tests under
`tests/unit/local_web/`. Add only the `serve` command to `src/repotrial/cli.py`.
Update the README with the launch command and truthful scope. Existing API and
trial-engine code are not implementation targets.

Interfaces: `create_app(workspace: Path, *, runner=..., doctor=...) -> FastAPI`
is the injectable local composition root; the runner owns child process lifetime
and bounded job state. Final argument names may follow existing typed conventions.

- [ ] Write and observe RED tests before production code: POST with wrong token
  or external Origin must be rejected; a second active job returns 409; the CLI
  argv preserves the exact SHA/port/path and never has `shell=True`; invalid or
  missing terminal artifacts cannot produce completed success.
- [ ] Implement runner with real asyncio subprocess boundaries, inherited model
  key only, bounded stdout/stderr draining, and SIGINT+wait during shutdown.
- [ ] Add route tests for loopback Host, validated request schema, owned report
  access and symlink refusal, idle-only doctor and responsive job polling.
- [ ] Implement the form with URL/SHA/port/Compose/model settings, real status,
  elapsed time and report links. Use textContent for runtime data and a sandboxed
  report view; do not ship a mock-success production mode.
- [ ] Add module entry point and loopback-only command. Test `serve` dispatch
  using a patched uvicorn boundary and run installed-wheel `--help` afterwards.
- [ ] Run `.venv/bin/uv run pytest -q tests/unit/local_web` plus affected CLI
  tests; `ruff check .`, `ruff format --check .`, and `mypy src/repotrial`.
- [ ] Controller reviews one completed diff, dispatches one Sol module review,
  and runs a trusted local-browser smoke. Fix concrete findings only.
- [ ] Build wheel and verify included template/entry point; run final proportional
  regression gates. Commit only scoped files under Owner identity after approval
  of the module gate. No push or publication.

## Controller preflight ruling

The UI and runner share request/job contracts and are one review unit, not
parallel implementation tasks. CLI artifacts are reused so local users do not
need PostgreSQL; this does not silently replace the durable production API.
No automatic resume, granular graph progress or full browser validation is claimed.

## Delivery verification (2026-09-07)

Implementation and the concentrated Sol review are complete; controller verified
the three blocking corrections and nonblocking FIFO rejection. Full regression:
2142 passed, 12 skipped. Subsequent test-only additions: 76 focused/CLI tests
passed; coverage appended on unchanged production code reached 85.0071%.
Ruff, formatting, mypy, and diff checks passed. Installed-wheel Chromium smoke
verified the trusted UI fixture only, including script-blocked report access.
Both existing canary reports were read successfully; no new real canary ran.
This is a local preview, not a stable release or a fresh-machine setup proof.
