# M7.5 release-gap closure evidence

This ledger is append-only. Historical Pilot evidence remains unchanged.

## Task 1 - Linux baseline and preflight

- implementation_base_sha: `b19dd13f632255caeee7338d773f84a4b3eef97e`
- code_diff_base_sha: `b2dc2c25dd59b3b3e99c337552372e0896aee004`
- result: `ENVIRONMENT_BLOCKED`
- execution_worktree: `/home/repotrial/src/RepoTrial-m7.5-release-gap`, clean ext4 (`ext4 /dev/sdd /`), exact implementation base
- environment: Windows `10.0.26200.9168`; WSL `2.6.3.0`; Ubuntu `24.04.4 LTS`; kernel `6.6.87.2-microsoft-standard-WSL2`; systemd; `/dev/kvm` accessible (`character special file 660 root:kvm`); `nproc=24`; memory `7804 MiB total`, `6463 MiB available`; state volume `106788 MiB available` of `120380 MiB`
- manifest_sha256: `4a963f0ee730ddf4be7243ad31cc2899ce19cdbfed3c4570498ff7d1c617551a`
- uv_lock_sha256: `e50aad2994ad2bc895f95acb4cba56d38a2e9bcbb8a97fecb7deb07dfd9f4e39`
- sandbox_package_sha256: `bf36b1ac0a8daf5ee2ff44d138cfba578b3af6812733056c8000982b184f1631`
- repository_gates: `uv 0.11.5` from `/home/repotrial/.local/share/repotrial-uv/bin/uv`; `uv lock --check` PASS; `uv sync --locked --all-groups` PASS; Ruff lint PASS; Ruff format `FAIL` because the approved plan Markdown code block is unformatted at `docs/superpowers/plans/2026-09-02-m7.5-release-gap-closure.md:274`; mypy PASS; full pytest `1519 passed, 6 skipped, 1 warning`; branch coverage `87.29%` (threshold `>=85%`)
- sbx_preflight: `sbx version` PASS (`v0.39.0`, CLI commit `def8cb0523a77e757bdd6ef52b459fe374f3783e`); `sbx list` empty after the previously authorized stale sandbox disappeared; `sbx diagnose` `11 passed / 1 warning` (warning is GitHub update check timeout), no sandbox remains; trusted calibration `REPOTRIAL_RUN_WSL2_SBX_CALIBRATION=1` returned `2 passed, 1 failed in 397.58s`, with the timeout calibration hitting SBX clone-duration/network-lock deadlines and final inventory empty
- model_endpoint_class: `public OpenAI-compatible`
- model_name: `deepseek-v4-flash`
- model_credentials_state: `REPOTRIAL_MODEL_API_KEY=SET`, `REPOTRIAL_MODEL_ENDPOINT=SET`; no credential values recorded
- pid_hard_bound: `unsupported`
- blocking_reason: Ruff format baseline and trusted calibration did not fully pass; no real repository results are inferred

## Task 1 corrected rerun and ruling (append-only)

Historical Task 1 entries above are retained unchanged.

- task_id: `Task 1`
- BASE_SHA: `b19dd13f632255caeee7338d773f84a4b3eef97e`
- HEAD_SHA: `ece0b206dd42151b1c84c07a7177c3cfa92bb78b` (exact execution HEAD
  observed before this evidence-only append)
- corrected_rerun_parent_sha: `ece0b206dd42151b1c84c07a7177c3cfa92bb78b`
- historical_commits: `4861a33b826082c5f31ccc5c94df322ba757e5c9`
  (`docs: record M7.5 Linux baseline`) followed by
  `ece0b206dd42151b1c84c07a7177c3cfa92bb78b`
  (`docs: complete M7.5 Linux baseline evidence`)
- changed_files: `docs/dev/pilot-evidence/m7.5-release-gap-closure.md`,
  `docs/dev/status.md`, and ignored report artifact
  `.superpowers/sdd/2026-09-02-m7.5-release-gap-closure/task-1-report.md`;
  production changed_files: none.
- hypothesis: admit Task 1 only when reviewed base/ref, Linux runtime, locked
  gates, and trusted SBX route all match the plan; any mismatch is
  `ENVIRONMENT_BLOCKED`.
- root_cause_evidence: planning ref `codex/m7.5-compatibility-recovery`
  resolved to `a40e9c94433fed662af1f7f2d6e8f6cd9de49e7b` with subject
  `docs: record M7.5 release baseline`, not the required
  `docs: add M7.5 release-gap implementation plan`; reviewed execution base
  `b19dd13f632255caeee7338d773f84a4b3eef97e` is an ancestor of approved
  `1f46388dbf502e091d0475c2d7ee71540614ae49`; default-PATH pytest could not
  spawn `uv`; Ruff format and trusted calibration remained failing gates.
- python312_fingerprint: `python3.12 --version` -> `Python 3.12.3`;
  executable `/usr/bin/python3.12`; binary SHA-256
  `a92f0f95e883390c7256b2e441484aac06b1002dbe1d924141a77c8d82f96223`.
- guest_compose_fingerprint: `Docker Compose version v5.5.0` from trusted
  disposable-fixture calibration; current corrected route stopped before a new
  guest Compose query.
- retained_calibration_fingerprints: runtime-input
  `3598b61c3fca786bfcf9b81f9e5d030ea38613f13736ecb92bd5d14d7feabcff` and
  environment `521b50be7c0ca064799f1f0f7e1cdd478fddc765d9ab6d5f8c49d2e26d13b005`,
  both from trusted calibration HEAD `760f73c15aba839a71ac036699b527334808b73f`
  and retained as provenance only.
- policy_fingerprint: canonical active global network rule is
  `default-deny-all` over `**` with decision `deny`, immutable; canonical
  network JSON SHA-256
  `e745ff8da1066eba64b0bbf7865ff91a308b0b216f81c5c375091e0a13b46d8b`.
- free_m: Mem `7804 724 6794 3 480 7079`; Swap `2048 0 2048`.
- df_pm: header `Filesystem 1048576-blocks Used Available Capacity Mounted on`;
  all three rows are `/dev/sdd 120380 7430 106791 7%`, mounted respectively at
  `/`, `/home/repotrial`, and
  `/home/repotrial/.local/state/sandboxes`.
- RED: `not-applicable: evidence-only task`.
- GREEN/scoped: `uv lock --check` PASS exit `0`; `uv sync --locked --all-groups`
  PASS exit `0`; `uv run ruff check .` PASS exit `0`; `uv run mypy src/repotrial`
  PASS exit `0`; adjusted-PATH `uv run pytest -q` PASS exit `0`
  (`1519 passed, 6 skipped, 1 warning`, `73.67s`); adjusted-PATH coverage
  PASS exit `0` (`1519 passed, 6 skipped, 1 warning`, branch `87.29%`,
  `104.78s`).
- full_gates: `uv run ruff format --check .` FAIL exit `1` due approved plan
  Markdown lines `274-307`; initial default-PATH `uv run pytest -q` FAIL exit
  `1` with `1518 passed, 6 skipped, 1 failed` from subprocess
  `FileNotFoundError` for `uv`; `git diff --check` PASS for this append.
- SBX_result: `sbx version` PASS exit `0`
  (`v0.39.0 def8cb0523a77e757bdd6ef52b459fe374f3783e`); current read-only
  `sbx diagnose` PASS exit `0` (`12 passed`); `sbx list` PASS exit `0` with
  exact empty inventory `No sandboxes found.\nLaunch one: sbx run claude\n`;
  no daemon lifecycle mutation or host Docker fallback.
- model_result: `REPOTRIAL_MODEL_API_KEY=SET`,
  `REPOTRIAL_MODEL_ENDPOINT=SET`, endpoint `public OpenAI-compatible`,
  model `deepseek-v4-flash`; no secret values recorded.
- calibration_command: brief marker
  `REPOTRIAL_RUN_REAL_SBX=1 uv run pytest tests/integration/sandbox/test_wsl2_linux_sbx_calibration.py -q -s`
  -> `3 skipped in 0.03s`, exit `0`; code marker
  `REPOTRIAL_RUN_WSL2_SBX_CALIBRATION=1 uv run pytest tests/integration/sandbox/test_wsl2_linux_sbx_calibration.py -q -s`
  -> `1 failed, 2 passed in 397.58s`, exit `1`.
- failed_test: `tests/integration/sandbox/test_wsl2_linux_sbx_calibration.py::test_wsl2_linux_sbx_timeout_calibration`;
  `DockerSbxError: clone_verification failed: total_duration_exhausted`;
  final inventory empty, with Docker Hub refresh-lock `context deadline exceeded`
  warning; two other trusted tests passed.
- review_disposition: Important review omissions are addressed in this
  append-only corrected rerun; historical attempts remain intact; controller
  review is the final acceptance gate.
- known_limitations: plan Markdown format failure; default-PATH uv subprocess
  discovery issue; trusted SBX clone/network-lock timeout; planning-ref and
  brief/code calibration marker mismatch; PID hard bound `unsupported`;
  current-head guest Compose execution not reached; no real repositories run.
- controller_ruling: `ENVIRONMENT_BLOCKED`. Preserve the branch and evidence;
  do not proceed to real repositories or claim release readiness.

## Task 1 proxy-causal rerun (append-only)

- task_id: `Task 1`; parent_sha: `5c8265987fc85613f0247755b32ede3a5371570f`;
  production_changed_files: none.
- hypothesis: the combined calibration failure is caused by host `sbx` CLI
  Docker Hub refresh/network waits when the WSL test process has no proxy;
  explicit routing through the configured proxy should remove that external
  delay without changing RepoTrial behavior.
- evidence: without proxy, the full run returned `2 failed, 1 passed in
  472.21s`; failures were `total_duration_exhausted` in primary `exec` and
  timeout cleanup reported `could not acquire docker hub refresh lock`.
  Direct WSL `curl` to Docker Hub also timed out without a proxy, while the
  configured upstream `172.24.64.1:7890` returned Docker Hub responses.
- controlled reruns: explicit environment
  `HTTP_PROXY/HTTPS_PROXY/ALL_PROXY=http://172.24.64.1:7890` produced primary
  `1 passed, 2 deselected in 115.61s`; the complete calibration then produced
  `3 passed in 270.40s`.
- sbx_state: daemon healthy, inventory empty after the run; no RepoTrial
  source, test, manifest, README, or target repository changed.
- controller_ruling: Task 1 SBX calibration gate is satisfied only when the
  WSL execution environment exports the configured proxy; preserve the prior
  no-proxy failure as historical evidence and use this explicit environment
  for subsequent supported calibration/canary commands. Do not add retries or
  production proxy behavior under this evidence-only task.

## Task 1 final gate ruling (append-only)

- final_head_sha: `ebaa984418addeeea55875308f9913d57fd293bb`
- fresh_controller_verification: `uv lock --check`, Ruff lint/format, mypy,
  and full pytest all passed (`1519 passed, 6 skipped, 1 warning`); `sbx
  diagnose` passed and `sbx list` is empty.
- trusted_calibration: with the configured WSL proxy exported, complete suite
  passed (`3 passed in 270.40s`).
- independent_review: three fresh `gpt-5.6-luna` max dispatches failed before
  producing a report with external HTTP `502/503`; no reviewer verdict is
  claimed. Controller performed a read-only diff/package review and found no
  Critical or Important issue in this evidence-only append.
- controller_ruling: Task 1 requirements are met under the explicit proxy
  environment; retain the external-review outage as a recorded process
  deviation and require the next available independent review before branch
  integration. Proceed to Task 2 using the same proxy-backed WSL execution
  environment; no production behavior was changed.

## Task 2 - RG1 Docker-data capacity diagnostic (controller execution)

- task_id: `Task 2`; base_sha: `2367ba426fbe32aa0969c0143fb9b237f3e8a58d`;
  production_changed_files: none.
- implementer_process_deviation: three fresh `gpt-5.6-sol` dispatches were
  rejected by the external relay with HTTP `503 Service Unavailable` before
  producing a report. The controller used the already-reviewed bounded runner
  and recorded this deviation; no production behavior was changed.
- admission: WSL state volume `/dev/sdd`, total `120380 MiB`, available
  `106783 MiB`, current sandbox state usage `2234 MiB`, inventory empty;
  allocation control remained `2048 -> (1,1919,128)`, `4096 -> (1,3839,256)`,
  `8192 -> (1,7679,512)`.
- runner: `docs/dev/pilot-evidence/scripts/m7.5_disk_probe.py`, SHA-256
  `967970ba7f58af4c96b6f4775857e06ca812a9495dd434574d8c19ee5674274f`;
  accepts only `4096` or `8192` and injects the diagnostic policy through the
  provider factory. Model class/name: public OpenAI-compatible /
  `deepseek-v4-flash`; no credentials recorded.
- `4096` Uptime Kuma: run `9a41bd43-16e7-41b6-a55a-f8dc639ac3de`, exact SHA
  `a852e21eba4ecf339624b404518c5bc7fad6d45c`, compose `compose.yaml`, port
  `3001`, `179.24s`; boot/health succeeded, required Journey coverage was
  insufficient (`exit_code=3`), terminal `trial_failed`, cleanup succeeded,
  and post-run inventory was empty.
- `4096` n8n: run `0dbec259-89aa-4fc5-955b-94fb232dd4b7`, exact SHA
  `6b78193475d84ae190622d8a8e9ba8598e89b7d1`, compose
  `docker-compose/withPostgres/docker-compose.yml`, port `5678`, `197.16s`;
  boot extraction crossed the Docker-data boundary with `no space left on
  device`, terminal `boot_recovery_stopped`, cleanup succeeded, and post-run
  inventory was empty.
- ruling: because n8n retained Docker-data ENOSPC at `4096 MiB` and fresh
  admission remained healthy, continue with the same two repositories at
  `8192 MiB`, strictly serially. Do not change policy defaults or public CLI
  options.
- `8192` Uptime Kuma: run `db7dcfa3-64be-4e3c-b86e-416486fb5e2a`, exact SHA
  `a852e21eba4ecf339624b404518c5bc7fad6d45c`, compose `compose.yaml`, port
  `3001`, `210.37s`; boot/health succeeded, required Journey coverage was
  insufficient (`exit_code=3`), terminal `trial_failed`, cleanup succeeded,
  and post-run inventory was empty.
- `8192` n8n: run `8bbd34be-af44-4c3a-b500-f3446c11ce70`, exact SHA
  `6b78193475d84ae190622d8a8e9ba8598e89b7d1`, compose
  `docker-compose/withPostgres/docker-compose.yml`, port `5678`, `258.64s`;
  the bounded run ended with `DockerSbxError`, `sandbox:exec:timeout`,
  `exit_code=4`, no report was produced, cleanup succeeded, and post-run
  inventory was empty.
- state-volume-after: `/dev/sdd`, available `106740 MiB`, usage `2237 MiB`;
  cleanup reserve remained available and no sandbox leaked.
- `RG1_CAPACITY_GATE=FAIL no_common_bounded_total_through_8192`.
- `DISK DEFAULT CHANGE PROPOSED - OWNER DECISION REQUIRED` is not emitted:
  no common proven candidate exists, so current default `2048 MiB` remains
  unchanged and no RG1 policy design is created.
- controller ruling: Task 2 diagnostic execution is complete with a FAIL
  capacity gate. Do not advance to a policy change or claim release readiness;
  retain all four bounded run artifacts and their failure/cleanup evidence.

### Task 2 evidence field completion

The following is the controller's exact per-run audit index. The runner was
invoked through `uv run python` with the proxy variables supplied by the
execution environment; no environment values are recorded.

| total | repository | exact invocation argv (after runner path) | UTC start -> end | R/D/W MiB | state volume df/du before; peak; after | guest df | lifecycle/cleanup |
|---|---|---|---|---|---|---|---|
|4096|uptime-kuma|`uptime-kuma https://github.com/louislam/uptime-kuma a852e21eba4ecf339624b404518c5bc7fad6d45c compose.yaml 3001 4096 https://api.deepseek.com`|`05:17:19.408139Z -> 05:20:26.134180Z`|`1/3839/256`|`df avail=106783 MiB, du=2234 MiB; peak=not_observed; after df avail=106763 MiB, du=2235 MiB`|`not_observed`|create_success; destroy_success; inventory empty|
|4096|n8n-hosting|`n8n-hosting https://github.com/n8n-io/n8n-hosting 6b78193475d84ae190622d8a8e9ba8598e89b7d1 docker-compose/withPostgres/docker-compose.yml 5678 4096 https://api.deepseek.com`|`05:22:51.055325Z -> 05:26:13.049441Z`|`1/3839/256`|`df/du before=not_observed; peak=not_observed; after df avail=106761 MiB, du=2235 MiB`|`not_observed`|create_success; destroy_success; inventory empty|
|8192|uptime-kuma|`uptime-kuma https://github.com/louislam/uptime-kuma a852e21eba4ecf339624b404518c5bc7fad6d45c compose.yaml 3001 8192 https://api.deepseek.com`|`05:41:46.064066Z -> 05:45:21.541748Z`|`1/7679/512`|`df/du before=not_observed; peak=not_observed; after df avail=106740 MiB, du=2237 MiB`|`not_observed`|create_success; destroy_success; inventory empty|
|8192|n8n-hosting|`n8n-hosting https://github.com/n8n-io/n8n-hosting 6b78193475d84ae190622d8a8e9ba8598e89b7d1 docker-compose/withPostgres/docker-compose.yml 5678 8192 https://api.deepseek.com`|`05:46:30.057268Z -> 05:51:01.134906Z`|`1/7679/512`|`df/du before=not_observed; peak=not_observed; after=106740 MiB / 2237 MiB`|`not_observed`|create/exec timeout; cleanup succeeded; inventory empty|

Admission was measured before the first dequeue at `df avail=106783 MiB`,
`du=2234 MiB`; the post-queue measurement was `106740 MiB` available and
`2237 MiB` used. A numeric fixed-overhead observation separate from state
usage was not exposed by SBX (`not_observed`). The conservative cleanup-reserve
calculation therefore uses the measured 2234 MiB state baseline plus the
selected maximum 8192 MiB: `10426 MiB` required, with more than 106 GiB free
at admission. Each dequeue was preceded by the exact empty inventory shown
above. Guest filesystem `df` and live peak state-volume samples were not
exposed in the retained artifacts; these are explicitly `not_observed`, not
interpreted as zero or success.

Ruling: the missing peak/guest observability and the 8192 n8n timeout are
additional reasons the result cannot establish a common proven capacity; the
previous `RG1_CAPACITY_GATE=FAIL` remains unchanged.

## RG1 capacity evidence correction (append-only; 2026-09-03)

This erratum supersedes the previous RG1 gate interpretation without
rewriting the historical run record. Independent review of the retained raw
evidence confirms:

- At `4096 MiB`, Uptime Kuma crossed the capacity boundary.
- At `4096 MiB`, n8n retained Docker-data `ENOSPC`.
- At `8192 MiB`, both Uptime Kuma and n8n crossed image pull/extraction. n8n
  `Boot up`, `ps`, and `logs` all exited `0`.
- The later n8n `sandbox:exec:timeout` was a post-Boot observation failure in
  `docker diff`; it cannot negate the capacity result. The overall n8n trial
  nevertheless failed, and it must not be counted as autonomous success.

The final RG1 capacity verdict is:

```text
RG1_CAPACITY_GATE=PASS common_total_mib=8192
DISK DEFAULT CHANGE PROPOSED - OWNER DECISION REQUIRED
current_default_mib=2048
proven_candidate_mib=8192
state_volume_required_mib=10426
maximum_supported_concurrent_target_sandboxes=1
choices=retain-2048 | approve-global-proven-candidate | retain-2048-and-request-separate-per-run-design
```

`state_volume_required_mib=10426` is a conservative admission/reserve upper
bound, derived from the measured `2234 MiB` state baseline plus the selected
`8192 MiB` total; it is not a live peak measurement. Live peak state-volume
usage, guest `df`, and independent fixed SBX overhead remain `not_observed`.

Review/process record: Task 1 independent review is `content APPROVE`; the
historical review-service/process deviation remains retained. Task 2's
original review verdict was `CHANGES REQUESTED`. This append-only submission
corrects the erroneous interpretation only; an independent review of this
Task 2 correction is still required.

No RG1 policy plan is created by this correction, and no CLI/API default is
changed; the current default remains `2048 MiB` pending the Owner decision.

## RG1 policy default and same-HEAD affected canary (append-only; 2026-09-03)

- task_id: `RG1 policy default`.
- owner_decision: `approve-global-proven-candidate`; the normal CLI and API
  Docker SBX default is changed from `2048 MiB` to `8192 MiB`. No public
  per-run disk option or provider/allocation contract was added.
- plan_chain: capacity correction
  `32070b66cdfb35dbccec5348f6e08e60f8be252b`; initial narrow plan
  `7e70860c1795725ebae6d5a06a37bc68cff96129`; review-corrected plan
  `8821cf42afe09895b53ab6a1d4871faafa4fc4ad`.
- implementation_base_sha: `8821cf42afe09895b53ab6a1d4871faafa4fc4ad`.
- reviewed_policy_head_sha / canary execution identity:
  `811fd25a5416433e3db587d71d4032ae66be3e40`.
- implementation_diff: exactly four `2048 -> 8192` replacements in
  `src/repotrial/cli.py`, `src/repotrial/api/app.py`,
  `tests/unit/test_cli.py`, and `tests/unit/api/test_app.py`. CPU, memory,
  PID disclosure, total duration, network policy, public interfaces,
  `calculate_disk_allocation()`, provider environment propagation, manifest,
  and README are unchanged.
- TDD: the two updated entry-point assertions first failed exactly as
  `actual 2048 != expected 8192`; after the two production literal changes,
  the focused pair passed. Both complete entry-point unit files passed
  (`128 passed`), the explicit `4096 MiB` policy remained unchanged,
  `calculate_disk_allocation(8192) == (1, 7679, 512)` with hard sum `8192`,
  and the existing official environment propagation test passed.
- gates: Ruff lint PASS; Ruff format PASS (`122 files already formatted`);
  mypy PASS (`44 source files`); full pytest PASS (`1519 passed, 6 skipped`);
  branch coverage PASS (`87.29% >= 85%`); exact range/working-tree
  `git diff --check` PASS. The first full-test invocation lacked the installed
  uv directory in subprocess `PATH` and failed only the packaging spawn test;
  the corrected-PATH full command is the accepted fresh result.
- review_process: the independent plan review rejected three evidence-flow
  defects; commit `8821cf42` corrected durable reporting, Observer-boundary
  capture, and the unreproducible project-local pre-commit requirement. Two
  internal WSL implementer attempts and one read-only code-review attempt were
  closed after producing no applicable WSL change/verdict. No independent
  code-review approval is claimed; the controller performed the exact
  four-line diff review and fresh gates. This process deviation remains open
  for final branch integration review.

### Uptime Kuma affected canary

- repository/SHA/path/port:
  `https://github.com/louislam/uptime-kuma` /
  `a852e21eba4ecf339624b404518c5bc7fad6d45c` / `compose.yaml` / `3001`.
- run_id: `05c34388-3da0-4c10-8951-09cf3904c626`; UTC
  `2026-09-03T10:37:21Z -> 2026-09-03T10:40:28Z`; monotonic duration
  `186.4282267660019s`; raw exit code `3`.
- exact SHA verification PASS. Boot PASS with service
  `running/healthy`. Observation discovery/inspect/diff/top/network/audit all
  completed successfully.
- required Journey set was non-empty (`1`). `GET /` expected `200` but
  received `302`, so the Journey failed. Report was produced with the exact
  commit identity; terminal outcome `trial_failed`; exact stop reason
  `insufficient_coverage`.
- lifecycle: create success -> destroy success; post-run official inventory
  exactly empty. Artifact-hash manifest has seven entries and SHA-256
  `dab120f68409f4032c4a84c88eb7287e63601121878886ea50a7037c3a35354b`.
  Captured stdout SHA-256 is
  `70d0a5f4eeadd2a9e885ff1dba177521f657d955038d505fce92b5f5db2bcc9a`;
  stderr is empty with SHA-256
  `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`.

### n8n affected canary

- repository/SHA/path/port: `https://github.com/n8n-io/n8n-hosting` /
  `6b78193475d84ae190622d8a8e9ba8598e89b7d1` /
  `docker-compose/withPostgres/docker-compose.yml` / `5678`.
- run_id: `41332b89-9090-4e61-8036-1f59dfdcd6ec`; UTC
  `2026-09-03T10:41:59Z -> 2026-09-03T10:46:08Z`; monotonic duration
  `247.46408897999936s`; raw exit code `4`.
- exact SHA verification PASS. Boot PASS: `n8n` and `n8n-runner` were
  running and `postgres` was `running/healthy`.
- post-Boot Observer discovery and inspect succeeded; `diff` then failed with
  `DockerSbxError`. The public stop reason is `sandbox:exec:timeout`.
  Lifecycle evidence records `deadline_limited=false`, trial elapsed
  `235.392841s`, and trial remaining `664.607159s`, so this is the retained
  per-command Observer timeout rather than whole-trial deadline exhaustion.
  No Journey execution result or report was produced at that boundary.
- lifecycle: create success -> provider failure -> destroy success; post-run
  official inventory exactly empty. Artifact-hash manifest has five entries
  and SHA-256
  `cf4138293a52d25f1e014adf3abe978e9229313d10ee16a10f5a504733823790`.
  Captured stdout SHA-256 is
  `c56e27fadac7a891f417b83828a31d424ef889cac1e8fa672b857301e25a4901`;
  stderr SHA-256 is
  `f5e6b3e5798c61be221b7948114f21eb6526fd9278570755df56b9888d4a5db5`.

### RG1 policy ruling

- both repositories crossed image pull/extraction and Boot under the normal
  `8192 MiB` CLI default. The capacity/default-policy objective is verified.
- same-HEAD autonomous recovery count is `0/2`: Uptime retained a deterministic
  Journey mismatch (`302` vs `200`), and n8n retained the post-Boot Observer
  `diff` timeout. Neither result is reinterpreted as success.
- final state volume: `/dev/sdd`, `106621 MiB` available, state usage
  `2238 MiB`; official sandbox inventory empty. No daemon lifecycle command,
  host fallback, repository-specific workaround, manifest/README change,
  push, or merge occurred. PID hard bound remains unsupported and disclosed.
- next work must treat Uptime redirect semantics and n8n Observer timeout as
  separate generic compatibility domains; this RG1 task does not fix either.

## RG4 Wakapi Boot-operation timeout diagnosis (append-only; 2026-09-03)

- diagnostic HEAD: `22af2736dd3cf24259f83372ab1c7b2942b72505`;
  production code, manifest, README, and frozen repository identity remained
  unchanged. The trusted diagnostic adds only a test fixture and integration
  test before any timeout policy change.
- frozen Wakapi identity: `https://github.com/muety/wakapi` at
  `8143ca13ade1c14959be8a79c1d736d9889013e4`, `compose.yml`, container port
  `3000`.

### Trusted slow-Compose A/B

- Fixture contract pins BusyBox by digest, performs one controlled
  `RUN sleep 250`, starts an immediately healthy loopback HTTP service, and
  contains no privileged mode, host/socket mount, or network-mode override.
- At the current Boot-operation limit (`240s`), the Compose-up command failed
  as provider operation `exec` / reason `timeout` with
  `deadline_limited=false`, `subprocess_started=true`, measured operation
  elapsed `240.101603s`, trial elapsed `270.435471s`, and `629.564529s`
  remaining. Instrumentation captured the exact host argv and actual provider
  timeout `240`; the owned host child was killed and reaped with return code
  `-9`. The immediate Compose state was empty. The timeout exception exposed
  empty stderr and no stdout field; that output limitation is asserted rather
  than represented as captured output.
- In a fresh sandbox, the same Compose argv with a `300s` operation limit
  returned `0` after `256.528070s` and the service was `running/healthy`.
  Both fresh sandboxes asserted create success -> destroy success and the
  final official inventory was exactly empty. The strengthened real
  integration result was `2 passed in 573.72s`.
- Raw controller log SHA-256:
  `6950a02502afd45ba3bae72694969d6f53cb0fd191b676934a6ca540730cd61d`.

### Exact Wakapi rerun on the unchanged HEAD

- metric-bearing diagnostic run_id:
  `b14277fb-2ec9-42b3-bceb-8162e322d70c`; CLI exit code `4`; monotonic
  duration `377.945146s`; exact SHA verification passed.
- Baseline Boot `up` again failed as provider `exec` / `timeout` with
  `deadline_limited=false`, `subprocess_started=true`, trial elapsed
  `267.772101s`, and `632.227899s` remaining. Stop reason was
  `sandbox:exec:timeout`; no report was produced. Lifecycle recorded provider
  failure -> destroy success and the official post-run inventory was empty.
- The current timeout exception boundary retained an empty stderr and no
  partial stdout payload. This is recorded as an observability limitation; no
  output is reconstructed from memory. Attempt result, Boot evidence, and
  lifecycle SHA-256 values are respectively
  `a083f63eb27f4056288a87e948d8e8e87e03cdecb912c50da25f212929db582b`,
  `297f0eccfec99e43ecf5dbe54f8f94e4758a1678898b67919b5fce1afac3a589`,
  and `5ff8741096faf4aaf6fb9cd76ae546d9642d2b0ad91e341b965f646852f8488c`.

### Bounded Wakapi completion probe

- A non-metric-bearing trusted probe used the same fixed workspace and exact
  Compose-up category. It supplied only the three test values required by the
  declared Compose secrets and remained under the provider's `900s`
  whole-trial deadline.
- With an `840s` diagnostic ceiling, the Compose operation returned rather
  than timing out after `333.088104s`. Image pull, the full Wakapi Go build,
  image export, network/volume creation, and both container starts completed.
  The command then returned `1` with Compose reporting Wakapi `unhealthy`;
  the immediate follow-up state showed both services running with Wakapi
  health `starting`. PostgreSQL had reported healthy before Compose returned.
- This is not an autonomous-success claim. It proves that the target operation
  is bounded and that the `240s` boundary cuts off structured Compose
  failure/recovery evidence before the operation naturally returns. Cleanup
  recorded create success -> destroy success and exact empty inventory. The
  bounded result artifact SHA-256 is
  `b88affd11795b0ed32aa0d820a74c61499a1f49caeae492747247d7842144fb0`.

### RG4 evidence ruling

```text
RG4_ENTRY_GATE=NOT_PROVEN
current_boot_operation_limit_s=240
trusted_fixture_candidate_limit_s=300
wakapi_measured_return_s=333.088104
target_hung=false
cleanup_confirmed=true
autonomous_success=false
timed_out_target_argv_and_streams_complete=false
repeatable_successful_wakapi_interval=false
```

Independent review found that the exact timed-out Wakapi attempt did not retain
its host argv, host-child kill/reap result, post-timeout Compose state, or
partial stdout; the separate bounded probe cannot retroactively prove those
fields. It also returned nonzero only once, so it does not establish the
plan's repeatable successful Wakapi interval. RG4 therefore does not authorize
a production timeout change. The diagnostic proves a bounded post-240-second
return and preserves the later health failure, then defers RG4 while the frozen
sequence continues to RG5. Generic provider timeout changes, readiness-window
changes, repository-specific conditions, retries, and reinterpretation of the
health failure remain prohibited.

## RG5 Paperless clone-verification diagnosis (append-only; 2026-09-03)

- diagnostic HEAD: `bf230cf957bea0a1f6292749ed4f197c94378415`;
  production code, provider behavior, manifest, README, and frozen repository
  identity remained unchanged.
- frozen identity: `https://github.com/paperless-ngx/paperless-ngx` at
  `6f3945f11f1ff13ab90da76c26de37660ff1d497`, Compose path
  `docker/compose/docker-compose.postgres.yml`, container port `8000`.
- exact one-shot run_id: `285c7f1f-5a62-4f6e-855c-93909a8af82d`;
  CLI exit code `3`; monotonic duration `597.266569s`; expected and actual
  verified SHA matched exactly. The retained host workspace remained clean;
  its explicit `core.filemode` value was `true`.

### Clone-verification result

- The former `clone_verification:guest_status_not_clean` failure did not
  reproduce. Sandbox creation and exact clone verification completed, and the
  attempt crossed into baseline Boot. No clone-failure evidence record was
  emitted, so there are no current raw status/diff entries, guest Git-config
  facts, captured-byte payload, malformed state, or truncation flag available
  to prove a representation difference or authorize normalization.
- The prior malformed/truncated record cannot be promoted into a deterministic
  representation hypothesis after the same frozen repository and SHA pass the
  current verifier. No reset, ignored-status rule, normalization, or rerun was
  used.

### Later failure boundary, cleanup, and hashes

- Baseline Compose `up` returned `1`; `ps` and `logs` completed. Services were
  recorded running, while the webserver repeatedly stopped because
  `PAPERLESS_SECRET_KEY` was absent or still the default `change-me` value.
  Boot verdict was `fail`.
- Recovery evidence called the model once and ended after `180.013753s` with
  outcome `planner_timeout`. The retained terminal result was
  `trial_failed` / `boot_recovery_stopped`; a report was produced with `0/3`
  Journeys executed and no experiments.
- Lifecycle recorded create success -> destroy success. Before any subsequent
  sandbox creation, the official inventory was persisted at
  `2026-09-03T13:07:35Z` and was exactly empty; its SHA-256 is
  `6fd9395af1ca7f2e169f6a8a7b2febcde6b1dc463c679b8be1c7cf0de60e8de3`.
  Attempt, Boot, lifecycle, recovery-model, and report SHA-256 values are
  respectively
  `e019e19440639297f1430a831f0bf123ea7702e4220b1346aaa25caa7a46bd92`,
  `152f5cd1e81deb52e82c72092c910dfaf72697e65f805315e2fe4cf370d9761e`,
  `ac76d7b0efd03aab54a2c47dd0dc4dd1a9f0c9ada36c8fa8db5b1fabee430b40`,
  `e148f8969bd77f39f2105e288003d3c31fb48ba0d5e60ff43a425b08bdfc2108`,
  and `f7edef253ba2fe2508e59e4685905fe19b4c6b5cc1e3d3d6ca1b600a685344ec`.

```text
RG5_ENTRY_GATE=FAIL
former_clone_failure_reproduced=false
deterministic_platform_representation=none
normalization_authorized=false
exact_sha=true
cleanup_confirmed=true
```

RG5 therefore produces no clone-normalization plan or production change. The
new Paperless Boot/recovery failure is retained as a separate later-stage fact;
it cannot be folded into clone verification. Continue the frozen sequence to
RG6.

## RG6 NetBox dependency-readiness diagnosis (append-only; 2026-09-03)

- diagnostic HEAD: `c975ad113466098d602f74f5f38af54e757fedfb`;
  production code, provider behavior, manifest, README, and frozen repository
  identity remained unchanged.
- frozen identity: `https://github.com/netbox-community/netbox-docker` at
  `5adc62fe3fa65163c4ef63733bdcbd3e59b5c544`, Compose path
  `docker-compose.yml`, container port `8080`.

### Trusted delayed-dependency boundary

- RED was the expected missing `delayed_dependency/compose.yaml`. The final
  fixture uses two digest-pinned BusyBox services. The `db` process waits 70
  seconds before serving its health marker; `web` has a health-conditioned
  dependency on `db`. The marker is injected by a Compose config rather than a
  host bind. Neither service has a volume, privileged mode, host networking,
  Docker socket, or external secret.
- The proxy-backed real diagnostic used two fresh managed sandboxes. With
  `--wait-timeout 60`, Compose returned `1` after `70.209780s` with `timeout
  waiting for dependencies`; `db` was `starting` at `71.859563s` and
  `76.328094s`, then `healthy` at `80.970207s`. Because the failed Compose
  invocation had already stopped dependency scheduling, `web` remained
  unstarted in this first sandbox.
- With a fresh sandbox and `--wait-timeout 120`, Compose returned `0` after
  `86.890421s`, with both `db` and `web` healthy. Both lifecycle artifacts were
  asserted as create attempt/success followed by destroy attempt/success. The
  opt-in diagnostic passed `2` tests in `258.61s`; its log SHA-256 is
  `d5bd4f00ba470b9c78966caec431456b51ca290ff6d3d2eef65316031c993693`.
  The post-run official inventory was exactly empty; its SHA-256 is
  `6fd9395af1ca7f2e169f6a8a7b2febcde6b1dc463c679b8be1c7cf0de60e8de3`.
- A prior no-proxy diagnostic reached a transient second-create failure while
  fetching Docker's JWKS. The daemon remained healthy and inventory was empty.
  Direct JWKS and GitHub probes returned HTTP 200 once the already-required WSL
  proxy environment was exported; the final diagnostic above then passed. No
  production retry or proxy behavior is inferred from that environment event.

### Frozen NetBox rerun

- The one permitted run is `1c794f9e-af12-4742-b2a2-8cc7eb0f80d3`.
  Expected and actual SHA matched exactly. CLI exit code was `3`; retained
  attempt duration was `289.435250s`; terminal result was `trial_failed` /
  `boot_recovery_stopped`.
- Boot retained `up=1`, `ps=0`, and `logs=0`. Final service state was
  `netbox=exited`, while `netbox-worker`, PostgreSQL, Redis, and Redis cache
  were running/healthy. PostgreSQL twice reported ready to accept connections.
  NetBox's migration process then retained the exact terminal line
  `Killed ./manage.py migrate --no-input`; the worker subsequently observed
  missing relation `core_job`. The evidence does not include a trusted kernel
  OOM record, so the reason for the kill is not narrowed further.
- Recovery produced a policy-rejected unsafe proposal in `5.241856s` and
  correctly stopped. Lifecycle recorded create success -> destroy success.
  The official post-run inventory was exactly empty, with SHA-256
  `6fd9395af1ca7f2e169f6a8a7b2febcde6b1dc463c679b8be1c7cf0de60e8de3`.
- Attempt, Boot, lifecycle, recovery-model, and report SHA-256 values are
  respectively
  `5448308e88d21a1ea8d77cc5800730a871a06573f0f9d96f4fdf0e46a05e349e`,
  `397710354fbad2344afef18ee238b24535d70a1dcc4dd2ddf46587ff525348a4`,
  `d3eca87a4f30a87472fdb1fb8bdeeba862af29d06f97262875dfdeb43953a71d`,
  `493e14306b49715125dc21af1e9766f66581ab68752022dce292e9d6195cd9cf`,
  and `dc97b073353aece77247cf54ba65efed6b178821f49652e3bc184913dad2fd1f`.
- Independent review first rejected the fixture test because an unrelated
  nonzero Compose result in the same time window could satisfy its broad
  assertions. The test now requires exit code `1`, the exact `timeout waiting
  for dependencies` evidence, successful state probes, a pre-health `starting`
  observation, and strict `current return < dependency healthy` ordering. The
  strengthened real diagnostic passed, and the same reviewer returned
  `APPROVE` with no remaining Critical or Important finding.
- Fresh repository gates passed: Ruff lint and format, mypy across `44` source
  files, full pytest (`1521 passed, 8 skipped`), branch coverage `87.29%`
  against the required `85%`, all pre-commit hooks, and `git diff --check`.

```text
RG6_ENTRY_GATE=FAIL
trusted_fixture_after_60_before_120=true
trusted_fixture_expanded_success=true
netbox_after_60_before_120_success=false
netbox_configuration_or_data_failure_present=true
readiness_change_authorized=false
exact_sha=true
cleanup_confirmed=true
```

The trusted fixture proves that the current 60-second value can reject a
well-formed delayed dependency. NetBox does not satisfy the second half of the
frozen gate: it never became healthy and instead retained a killed migration
and incomplete schema. Therefore no shared readiness-window plan or production
change is authorized, no recovery is counted, and the frozen sequence proceeds
to RG2 startup-input diagnosis.

## RG2 Linkding startup-input diagnosis (append-only; 2026-09-03)

- diagnostic HEAD: `0ff11abb7b01ceb0bde14e423cbca1a8b0a3ff16`;
  production code, graph/state, provider, manifest, README, and frozen Linkding
  identity remained unchanged.
- RED produced three expected `FileNotFoundError` failures because the
  `startup_input_diagnostic` corpus was absent. GREEN added the ten required
  cases: valid, symlink source, size/entry overflow, duplicate key, invalid
  UTF-8, shell syntax, forbidden control key, secret-looking value, target
  escape, and pre-existing target. Nonportable bytes are content-addressed
  descriptors that the test reconstructs without adding a Windows-only
  symlink checkout requirement. The oversized recipe reconstructs both 65
  assignments and 65,650 bytes rather than trusting descriptor metadata. The
  corpus tests pass `3/3`; manifest SHA-256 is
  `6915a67d50cb09c1ceb4a46263f6360de3db008d23c901a549e712feebc5bd9c`.

### Disposable-guest boundary proof

- A final diagnostic used the production `clone_and_resolve()` intake path and
  provider argv only. It verified SHA
  `65813a75404b1319aca8b09700fadc0b15adabaf`, an empty guest Git status, and
  Compose service `linkding` with required service-level `.env`.
- `.env` was absent and its resolved parent remained the verified clone root.
  `.env.sample` was a regular, non-linked, UTF-8 file of `2090` bytes with
  SHA-256
  `12a0e13fc1b005ea484b96c666813d1a601de5ad85c9db82262b67950dcb10bf`.
  The diagnostic recorded 20 key names and only the value classes empty,
  boolean, integer, repository-relative path, and local literal. It recorded no
  values and created no guest file.
- Lifecycle was create attempt/success -> destroy attempt/success and the final
  official inventory was exactly empty. Inspection, lifecycle, and inventory
  SHA-256 values are respectively
  `8113050f90682b163145e83a52375cbc12efd3b77e7d2ad990afe7f54629ea14`,
  `71d0032ecf1033cfe0c5d6eae472afac13958a815d0ebd076db6471c03c86759`,
  and `6fd9395af1ca7f2e169f6a8a7b2febcde6b1dc463c679b8be1c7cf0de60e8de3`.
- Preliminary harness attempts that used hand-built clone layouts failed before
  workload execution at clone verification and cleaned to empty inventory.
  They are not used as RG2 evidence. The final proof above deliberately uses
  the same intake path as production.

### Frozen Linkding rerun

- The unchanged run is `5383344b-bcb7-4b2c-9cc0-ffffb8350d01`.
  Expected and actual SHA matched. CLI exit code was `3`, retained duration was
  `163.329216s`, and terminal result was `trial_failed` /
  `boot_recovery_stopped`.
- Boot retained `up=1`, `ps=0`, and `logs=0`. The exact `up` error says required
  workspace `.env` was not found. No service started. Recovery was correctly
  policy-rejected as unsafe after `115.473647s`.
- Lifecycle recorded create success -> destroy success. Final inventory was
  exactly empty with SHA-256
  `6fd9395af1ca7f2e169f6a8a7b2febcde6b1dc463c679b8be1c7cf0de60e8de3`.
  Attempt, Boot, lifecycle, recovery-model, and report SHA-256 values are
  respectively
  `2213f09c2311f496e498ad253ede2bbbb98e0e905a82b3bea5f8d5c781c915be`,
  `8a50040317091f36ab3c293d3a2b8a31fbb1f49caa2f61b15f3fd7266f998ec2`,
  `8c06653a4ba3d1f8fb2acb0e73e8bc3fcd9199476c54f3d56fcf7fea1f600705`,
  `b87591e74c8b4d2b1868822ea81ebb75f622a9129992d722771b4f6297b98784`,
  and `7cc3234ac22ea5c3e0a9900bb196230d2703fbaec6b191c977e645137531c5fa`.

```text
RG2_DIAGNOSTIC_GATE=PASS
required_service_env_file=true
target_absent_and_confined=true
bounded_regular_utf8_template=true
local_or_synthetic_value_policy_possible=true
trusted_fixture_shape=true
linkding_recovered=false
IMPLEMENTATION=BLOCKED_PENDING_OWNER_POLICY_DECISION
```

The diagnostic conditions pass, but all 20 Linkding keys use the `LD_` prefix
that the frozen deferred implementation rule currently rejects. Independent
review confirmed that this later policy conflict must not retroactively turn
the diagnostic into a failure and must not be bypassed by a repository-specific
allowlist. The separate design
`2026-09-02-m7.5-startup-input-materialization-design.md` therefore defines an
explicit Owner choice: retain whole-template rejection and stop this recovery
path, or omit all control-prefixed assignments from the guest file under a
closed generic rule while leaving host command-environment protections
unchanged. The design also treats root `.env` as both service env-file and
Compose-interpolation input and requires a confined resolved-config proof. No
implementation, recovery, or production result is claimed.

### RG2 Owner decision (2026-09-03)

The Owner approved Option B after the diagnostic/design gate. Implementation
may omit every normalized control key (`COMPOSE_*`, `DOCKER_*`, `DYLD_*`,
`LD_*`, and the exact control keys named in the design) from the generated
guest file while retaining the existing host command-environment rejection.
The approval does not authorize a repository allowlist, graph/state/public
contract change, or unvalidated Compose interpolation. RED-first implementation
remains subject to focused review, real-SBX proof, and the full repository gates.

## RG2 Option B implementation and real-SBX validation (append-only; 2026-09-04)

- The implementation chain through execution HEAD
  `0c7c44678d81b9889c683bde8aae8dcbf93b7553` is `15b2c615` (redacted
  startup environment observation), `6a308873` (baseline materialization),
  `6b7e7762` (complete terminal evidence), `0fc1666b` (candidate parity), and
  `0c7c4467` (baseline startup-identity verification for candidates).
- The implementation keeps the existing graph/state, Provider, public
  API/CLI, Journey, mutation, and KEEP/ROLLBACK contracts. It plans on the
  trusted host, materializes only after sandbox creation and before Boot,
  omits every closed control key from the guest file, validates the resolved
  Compose document inside the guest, and uses the same verified startup input
  for baseline and candidate attempts.
- Real SBX exposed one generic compatibility error in the planned fixed
  `/workspace` assumption: the official runtime uses a dynamic clone path.
  The bounded adapter now accepts only an absolute, normalized, non-root
  `pwd -P` whose value exactly matches `git rev-parse --show-toplevel`; the
  host parser revalidates that root before passing it to the fixed bind
  validator. No guest root path is retained in evidence.

### Trusted fixture proof

- With `REPOTRIAL_RUN_REAL_SBX=1`,
  `tests/integration/sandbox/test_startup_input_materialization.py` passed
  `2/2` in `248.42s` against SBX `v0.39.0` on the final code snapshot. The
  pinned unprivileged BusyBox
  fixture was created, materialized through the production path, became
  healthy on the checked-in default port `8080`, passed `exec echo ok`, and
  was destroyed. Official inventory was exactly empty before and after.
- The generated `.env` evidence reported mode `0600`, accepted only
  `APP_OPTIONAL`, omitted `LD_HOST_PORT` and lowercase `ld_preload`, and
  retained no values. Adapter and bind-validator SHA-256 values are
  `2e409d6d7c3ed2f72ba78d7e0712467351df54ef759952eedb4bf3f93fa47233`
  and
  `23e41330934144f7c1f017e1c16e9a34e90d374956de5273be247264d0b7e868`.
- Startup-input evidence and lifecycle SHA-256 values are respectively
  `93b2d2b12e7efb6cd318d07989c7b0b131ce702a658f829c91c1ca677bb5b827`
  and
  `ca63c32f1144d17ffd5b354a7d31c57da80371eceba8ae14f7f22f496d00d860`.
  Lifecycle was create attempt/success -> destroy attempt/success.

### Frozen Linkding canary

- A preliminary CLI invocation failed before run-layout creation because its
  shell-expanded endpoint argument was empty. It created no run and no
  sandbox, and is not the post-fix canary. Its console-log SHA-256 is
  `af1a405811630799e25117758304cee187809522df166715930edb2d310733b2`;
  no credential value was printed.
- The sole valid canary is run
  `8f4b9f78-e1de-46d6-b1c2-98ffa464c232`, requested exact SHA
  `65813a75404b1319aca8b09700fadc0b15adabaf`, Compose
  `docker-compose.yml`, port `9090`, the public OpenAI-compatible DeepSeek
  endpoint class, and model `deepseek-v4-flash`; no endpoint credential or
  secret value is retained. It ended before checkout or sandbox creation with
  exit code `4`, `RepoIntakeError`, and stop reason
  `intake:clone_timeout` after `125.083775s`; consequently
  `actual_verified_sha` is null and no startup-input, Boot, Journey,
  experiment, or report result exists for this run.
- Attempt-result, private intake-failure, and console-log SHA-256 values are
  respectively
  `b73a64167290afbff7102f905e0c35ed59e7c4c4f578b5c01e3ee2d35bb0423c`,
  `d7d01c1a3bae25f5227876f9c93c26384d90cfa4fb0259a319666c71df7782b2`,
  and
  `f182020cc375a9e93430b9591411919bbe2709f610dfac9b4c2668dc1babfc68`.
  Official inventory after the run was exactly empty.

```text
RG2_TRUSTED_FIXTURE=PASS
STARTUP_INPUT_MATERIALIZATION=PROVEN_IN_REAL_SBX
LINKDING_CANARY=intake:clone_timeout
LINKDING_RECOVERED=false
RECOVERY_COUNT_INCREMENT=0
NO_HOST_FALLBACK=true
FINAL_SBX_INVENTORY=empty
```

The trusted fixture closes the runtime-materialization proof for the generic
feature. The frozen Linkding repository did not reach that path, so it cannot
be counted as recovered or used to claim the four-recovery release gate.

### Focused review

- An independent Sol-high reviewer approved the implementation with zero
  Critical and zero Important findings. It verified the dynamic-root and bind
  confinement boundaries, fixed argv/scripts and hashes, artifact secrecy,
  baseline/candidate parity, cleanup, no host fallback, fixture evidence, and
  Linkding non-recovery attribution.
- Two Minor findings were resolved before commit. RED proved that DEL and a
  Unicode bidi-format character were accepted in a reported guest root; GREEN
  rejects Unicode `Cc` and `Cf` categories (`4/4` targeted tests). The evidence
  now also records the canary's public endpoint class and
  `deepseek-v4-flash` model without retaining a credential.
- Final focused verification passed `322` tests with one expected real-SBX
  opt-in skip. Full verification passed `1629` tests with `9` skips and one
  dependency deprecation warning. Branch coverage was `87.13%` against the
  required `85%`. Ruff lint/format, mypy, pre-commit, `git diff --check`,
  secret-pattern count, and final official empty-inventory checks all passed.

## RG3 guest-loopback publication diagnosis (append-only; 2026-09-04)

- Diagnostic BASE was `2e59549665f3384fd8e2fe1f60fed965c99674b9`.
  This task added only a pinned trusted fixture and opt-in integration test;
  production code, Provider behavior, graph/state, manifest, README, and
  frozen contracts were not changed.
- RED was the expected missing-test-file failure. The non-real GREEN run was
  `1 passed, 2 skipped`. The trusted real-SBX run was
  `2 passed, 1 skipped in 132.47s` against `sbx v0.39.0`; daemon health passed
  with one non-blocking version-update HTTP 403 warning.
- The trusted fixture used pinned unprivileged BusyBox, a read-only root,
  dropped capabilities, no host volumes/devices/socket, and guest-only
  `127.0.0.1:8080:8080`. Ordered observations were:
  `target_container_loopback=pass`, `sandbox_guest_loopback=pass`, and
  `provider_published_loopback=error(connection_reset_error)` for port 8080.
  The bounded observation artifact was mode `0600` with SHA-256
  `04cea296a3645ea1b8590fd5dce11dcd9cef3b751b346fb1b7a88d6cf36d081f`.
  Lifecycle create/destroy succeeded; lifecycle SHA-256 was
  `bd4119e6f1022d1b5d853378bb3fd337fc6832ee3cc92115b5f33ba027c23be6`.
- The single changedetection.io attempt requested exact SHA
  `5d9c7c6da76340597243e8163c4f2439237fa0e8` but exited during the production
  intake clone boundary after the fixed 120-second clone window. Pytest marked
  the exact target node failed, the owned checkout was removed, and no
  sandbox, Boot, workload, or three-probe target artifact was created. The
  retained process timing and cleanup shape are consistent with
  `clone_timeout`; because the command output was truncated by the controller
  transport, no stronger exact-error claim is made from this attempt.
- Official inventory was exactly empty after both commands. The target did
  not reach the three probes, so it cannot satisfy the frozen same-port entry
  predicate and cannot authorize a production compatibility change.

```text
RG3_TRUSTED_FIXTURE=PASS
trusted_target_container_loopback=pass
trusted_sandbox_guest_loopback=pass
trusted_provider_published_loopback=error(connection_reset_error)
changedetection_exact_sha_requested=true
changedetection_exact_sha_verified=false
changedetection_target_probes=not_observed
RG3_ENTRY_GATE=NOT_PROVEN
RECOVERY_COUNT_INCREMENT=0
NO_HOST_FALLBACK=true
FINAL_SBX_INVENTORY=empty
```

Ruling: preserve the trusted boundary evidence, do not infer the
changedetection result, do not create the optional loopback compatibility
design, and do not modify production behavior. Proceed only to the frozen
same-HEAD Stage 1 recovery recomputation.

### RG3 focused-review correction (append-only; 2026-09-04)

- Independent review requested changes because a generic `DockerSbxError`
  could be compressed into the same shape as an HTTP failure after successful
  publication. RED reproduced the unsafe ambiguity with a fake provider whose
  `publish_port()` raised `total_duration_exhausted`; GREEN records
  `provider_total_duration_exhausted` and rejects it as a loopback topology
  signature.
- The topology predicate now requires both internal probes to pass and the
  third probe to be an allowlisted network failure observed only after
  `publish_port()` returns a host port. Provider/deadline/contract failures and
  marker mismatches remain fail-closed. The four persisted fields are
  unchanged.
- The same correction verifies the trusted marker body exactly and makes
  fixture Git creation independent of global configuration, hooks, and commit
  signing with a 30-second process bound.
- The strengthened trusted real-SBX test passed in `89.94s`. It reproduced the
  same mode-`0600` observation artifact and SHA-256
  `04cea296a3645ea1b8590fd5dce11dcd9cef3b751b346fb1b7a88d6cf36d081f`.
  The new lifecycle SHA-256 is
  `ee2129d8ae238d064f11f1914679933c05776280334b4eacc9e97b53aff43a43`;
  create/destroy succeeded and official inventory was exactly empty.
- This test-reliability correction does not change the target result or gate:
  `RG3_ENTRY_GATE=NOT_PROVEN`, recovery increment `0`, no production design
  authorized.

### RG3 security-review correction (append-only; 2026-09-04)

- The second independent review rejected the default urllib redirect behavior:
  an untrusted target could redirect the host diagnostic away from the fixed
  published loopback endpoint. RED covered a metadata-address redirect; GREEN
  installs a no-redirect handler and treats the original 3xx response as proof
  that only the requested loopback port was reachable.
- Additional RED/GREEN cases require an integer host port in `1..65535`,
  explicitly normalize nested `URLError` reasons and
  `RemoteDisconnected`, reject Provider contract errors from the topology
  signature, and validate the exact four-field evidence shape before opening
  the artifact file. The trusted real test now asserts the complete three-row
  topology signature, not only the first two rows.
- Seven non-real tests pass with two expected opt-in skips. The strengthened
  trusted real-SBX run passed in `79.39s`, retained the same mode-`0600`
  observation SHA-256
  `04cea296a3645ea1b8590fd5dce11dcd9cef3b751b346fb1b7a88d6cf36d081f`,
  and produced lifecycle SHA-256
  `d61299e8bd16443ec13035db36474418bb64537aad3c89550d6097804846b44f`.
  Create/destroy succeeded and official inventory was exactly empty.
- The security correction is diagnostic-test-only. It does not alter the
  changedetection attempt, production behavior, or the unchanged
  `RG3_ENTRY_GATE=NOT_PROVEN` ruling.

### Task 7 final report contract (append-only; 2026-09-04)

```text
task_id=7-rg3-guest-loopback-diagnosis
BASE_SHA=2e59549665f3384fd8e2fe1f60fed965c99674b9
HEAD_SHA=edd7ad3eb76638e1f53f55358b6678fcd1d002d1
evidence_closure=docs-only commit containing this exact code-HEAD report
changed_files=tests/integration/sandbox/fixtures/loopback_publication/compose.yaml,tests/integration/sandbox/fixtures/loopback_publication/marker.txt,tests/integration/sandbox/test_loopback_publication_diagnostic.py,docs/dev/pilot-evidence/m7.5-release-gap-closure.md,docs/dev/status.md
environment=Ubuntu 24.04.4 LTS; WSL2 kernel 6.6.87.2; ext4; Python 3.12.3; uv 0.11.5; sbx v0.39.0 def8cb0523a77e757bdd6ef52b459fe374f3783e
environment_health=sbx diagnose 12 pass/0 warn/0 fail/0 skip; final inventory exactly empty
policy=cpus=1,memory_mb=1024,pids_limit=64,disk_mb=8192,total_duration_s=900; PID hard bound remains known unsupported
model=not_used_by_trusted_fixture_or_pre-probe_target_failure
hypothesis=guest-loopback publication works inside SBX while provider host publication resets
root_cause_evidence=trusted target-container and guest-loopback pass; publish_port returned a valid host port; fixed no-redirect host-loopback HTTP transport reset
RED_1=missing diagnostic test path
RED_2=provider total-duration error lacked a distinct fail-closed topology classification
RED_3=invalid port, redirect, nested URL/network exceptions, and pre-write evidence validation regressions
RED_4=HTTP 404/500 were misclassified as transport topology failures
GREEN=9 passed,2 skipped focused non-real diagnostic tests
real_sbx=trusted fixture PASS; exact marker; full topology signature; create/destroy PASS; observation sha256 04cea296a3645ea1b8590fd5dce11dcd9cef3b751b346fb1b7a88d6cf36d081f; final inventory empty
target=changedetection exact SHA requested but not verified; stopped in intake window before sandbox/Boot/probes; no rerun
ruff=PASS
ruff_format=PASS
mypy=PASS (45 source files)
full_pytest=1638 passed,11 skipped,1 warning
coverage=87.13 percent (required 85 percent)
pre_commit=PASS
git_diff_check=PASS
no_host_fallback=true
production_changed=false
manifest_or_readme_changed=false
RG3_ENTRY_GATE=NOT_PROVEN
RECOVERY_COUNT_INCREMENT=0
```

Review history: round 1 found Provider/network classification and fixture
reliability gaps; round 2 found redirect SSRF, port/exception, full-signature,
pre-write validation, and wording gaps; round 3 found HTTP-status reachability
and missing report-contract fields. All code findings received focused
RED/GREEN fixes. A fresh review of the exact final code HEAD and this closure
record remains the final Task 7 gate.

### Task 7 final independent review (append-only; 2026-09-04)

The fresh final Reviewer examined code HEAD
`edd7ad3eb76638e1f53f55358b6678fcd1d002d1` and evidence closure
`b981cae72b5ca7166ce025d504f94b4c6abb699b`. Verdict: `APPROVE`, with zero
Critical and zero Important findings. Task 7 is closed; its final ruling
remains `RG3_ENTRY_GATE=NOT_PROVEN` and recovery increment `0`.

```text
review_provenance=multi_agent_v1
review_agent_id=01a06acd-0c2b-7530-b791-263a21d46f99
review_model=gpt-5.6-sol
review_reasoning_effort=high
reviewed_code_head=edd7ad3eb76638e1f53f55358b6678fcd1d002d1
reviewed_evidence_head=b981cae72b5ca7166ce025d504f94b4c6abb699b
review_received_at_utc=2026-09-04T05:12:39.7317706Z
review_verdict=APPROVE
review_critical_findings=0
review_important_findings=0
```

## Task 8 same-HEAD Stage 1 recovery recomputation (append-only; 2026-09-04)

- Evaluated latest reviewed HEAD:
  `b981cae72b5ca7166ce025d504f94b4c6abb699b`; its production snapshot is
  `814c0bfff4cb33daafa20f0cd29ea6d8d79ae400`. Later commits through the
  evaluated HEAD add only Task 7 diagnostics/evidence.
- Method: recompute rather than accumulate. A row counts only when this one
  reviewed code snapshot has process success, a non-empty required Journey
  set with every Journey passing, a completed report, exact SHA, exact stop
  reason, cleanup PASS, and exact empty inventory. Historical results from a
  different HEAD cannot be carried forward.
- All seven latest retained rows fail at least one required predicate. No row
  is counted, so no same-HEAD or environment-fingerprint assertion is inferred
  for a recovered row.

| Former failure | Latest retained terminal fact | Missing autonomous-success predicate | Count |
|---|---|---|---:|
| uptime-kuma | `insufficient_coverage`; required Journey received `302` instead of `200` | process/Journey failed; historical HEAD | 0 |
| n8n-hosting | `sandbox:exec:timeout` during Observer `diff` | no Journey result or report; historical HEAD | 0 |
| wakapi | `sandbox:exec:timeout` during baseline Boot `up` | no Journey result or report; historical HEAD | 0 |
| paperless-ngx | `boot_recovery_stopped`; planner timeout after missing/default secret | Boot/Journeys failed; historical HEAD | 0 |
| netbox-docker | `boot_recovery_stopped`; killed migration and incomplete schema | Boot/Journeys failed; historical HEAD | 0 |
| linkding | `intake:clone_timeout`; actual SHA null | no verified checkout, sandbox, Journeys, or report | 0 |
| changedetection | one target diagnostic stopped in intake before three probes | exact SHA, target probes, Journeys, and report not observed | 0 |

```text
task_id=8-close-unmet-stage1-recovery-loop
BASE_SHA=b981cae72b5ca7166ce025d504f94b4c6abb699b
HEAD_SHA=b981cae72b5ca7166ce025d504f94b4c6abb699b
changed_files=docs/dev/pilot-evidence/m7.5-release-gap-closure.md,docs/dev/status.md
environment_and_policy=same reviewed Task 7 fingerprint; no target execution
RED=not-applicable: evidence-only task
GREEN=not-applicable: mechanical predicate recomputation
review=APPROVE; zero Critical/Important/Minor findings after provenance correction
known_limitation=no complete latest-HEAD seven-repository canary; strict rule therefore permits no historical accumulation
STAGE1_RECOVERY_GATE_UNMET
latest_reviewed_head=b981cae72b5ca7166ce025d504f94b4c6abb699b
recovery_count=0
unmet_repositories=uptime-kuma:insufficient_coverage,n8n-hosting:sandbox:exec:timeout,wakapi:sandbox:exec:timeout,paperless-ngx:boot_recovery_stopped,netbox-docker:boot_recovery_stopped,linkding:intake:clone_timeout,changedetection:target_probes_not_observed
scope_ruling=no broader compatibility work authorized
```

Controller ruling: stop before Task 9. Do not run the full cohort, mutation
work, or README release update. The release gate remains unmet; this is a
measured M7.5 limitation, not an environment-success claim.

Task 8 independent review provenance:

```text
review_provenance=multi_agent_v1
review_agent_id=01a06ad1-8c2a-7e80-a579-8a83b9342d1e
review_model=gpt-5.6-sol
review_reasoning_effort=high
reviewed_base_head=b981cae72b5ca7166ce025d504f94b4c6abb699b
review_received_at_utc=2026-09-04T05:15:04.8799983Z
review_round_1=CHANGES_REQUESTED:missing_task7_review_provenance
review_final_verdict=APPROVE
review_critical_findings=0
review_important_findings=0
review_minor_findings=0
```

## M7.5 continuation Stage A - baseline and diagnostic recertification

```text
implementation_base_sha=3031b591532ddcc22ee685843ea475fd01c32178
design_commit=440ef6f541b8b1b77bdf2f6fcaeb06b8f45062d6
lock_sha256=e50aad2994ad2bc895f95acb4cba56d38a2e9bcbb8a97fecb7deb07dfd9f4e39
manifest_sha256=4a963f0ee730ddf4be7243ad31cc2899ce19cdbfed3c4570498ff7d1c617551a
pytest_q=1638 passed, 11 skipped, 1 warning in 104.62s
pytest_cov=1638 passed, 11 skipped, 1 warning in 104.34s
branch_coverage=87.15%
sbx_version=v0.39.0 def8cb0523a77e757bdd6ef52b459fe374f3783e
sbx_diagnose=11 passed · 1 warning
sbx_diagnose_warning=remote update check timed out; daemon diagnostics and authentication passed on the final read-only rerun
sbx_list=No sandboxes found.
repository_gates=PASS
initial_inventory=empty
model_endpoint_class=public OpenAI-compatible
model_name=deepseek-v4-flash
model_api_key=SET
pid_hard_bound=unsupported
real_repository_execution=NOT_RUN
```

The first read-only `sbx diagnose` attempt returned `9 passed · 2 failed · 1
warning` because `Daemon diagnostics` and `Authentication` each hit the 5s
check timeout. The same command was rerun without changing the daemon and
returned `11 passed · 1 warning`; the remaining warning was limited to the
remote update check. The `sbx list` guidance line `Launch one: sbx run claude`
was not an inventory entry. No target repository or target Compose was run.

## M7.5 continuation Stage A - Task 2 diagnostic recertification (STOPPED; 2026-09-04)

Task 2 started from the reviewed Task 1 HEAD
`97605c56726ae77a10f5a12ba0fcb48f96d6f792` on the required isolated WSL
worktree. The two target attempts were required to run serially, but the
first target reached a cleanup failure, so the hard stop condition prevented
the second target from running. This section records only the retained first
attempt and does not replace any historical metric-bearing row.

```text
task_id=m7.5-stage-a-task-2
task_2_base_sha=97605c56726ae77a10f5a12ba0fcb48f96d6f792
code_head_before_evidence_commit=97605c56726ae77a10f5a12ba0fcb48f96d6f792
repository_id=linkding
role=diagnostic-only
code_head=97605c56726ae77a10f5a12ba0fcb48f96d6f792
run_id=37a85bd2-0034-437a-8de1-9968843b1684
requested_sha=65813a75404b1319aca8b09700fadc0b15adabaf
actual_verified_sha=65813a75404b1319aca8b09700fadc0b15adabaf
compose_path=docker-compose.yml
container_port=9090
exit_code=4
terminal_outcome=exception
stop_reason=internal:cleanuperror
monotonic_duration_s=131.71963241800404
target_workload_started=unknown
report_present=false
journey_count=not_observed
cleanup=FAIL
post_attempt_inventory=empty
attempt_result_sha256=c669aa676e4f6aa0de140ea63816811f4a6385771bf88ae64e926c015d06e116
lifecycle_sha256=c5ecf44886b3500ece4a6e7b7e3c30e3ad3c078fd780c45d781e06c57eb7c375
report_sha256=not_observed
startup_input_materialization=observed
startup_input_policy_id=startup-input-v1-option-b
startup_input_source=.env.sample
startup_input_target=.env
host_fallback=not_observed
daemon_mutation=NONE
```

The exact SHA was verified and the existing startup-input materializer emitted
its identity artifact. The artifact has no report paths, no executed Journey
result, and no Boot evidence. Its lifecycle records the create attempt,
`create_cleanup_unsafe`, a cleanup retry, and `cleanup_retry_failure`, each
with `DockerSbxError`; this is why cleanup is `FAIL` even though the final
official `sbx list` was exactly empty. The command used only
`--provider docker-sbx`; no host fallback was observed, but the attempt schema
does not provide a stronger independent host-fallback assertion.

The outer controller wrapper initially failed while parsing its `sed`
expression before producing any run artifact; its temporary log was empty and
the artifact inventory remained unchanged. After that non-target wrapper
failure was corrected, the single valid Linkding invocation produced the run
above. It was not rerun after the target result.

`changedetection.io` was **not run**. The continuation stopped immediately
after Linkding's cleanup failure, before the second admission gate. Therefore
there is no changedetection run ID, artifact, boundary, or result to infer.

Routing ruling: exact-SHA intake passed; Linkding reached the existing
startup-input materialization and requires no new startup policy; Boot/Journey
publication was not observed; cleanup failure is a hard stop. No target-side
three-probe plan, Uptime plan, production correction, metric recovery, or
release claim is authorized by this result. The next module is **not selected**
until the cleanup failure is adjudicated by the controller/Owner.

```text
STAGE_A_TASK_2=STOPPED
LINKDING_DIAGNOSTIC=RETAINED_CLEANUP_FAILURE
CHANGEDETECTION_DIAGNOSTIC=NOT_RUN_DUE_TO_HARD_STOP
EXACT_SHA_GATE=PASS_FOR_LINKDING
CLEANUP_GATE=FAIL
FINAL_SBX_INVENTORY=empty
NO_PRODUCTION_CHANGE=true
NO_TEST_CHANGE=true
NO_MANIFEST_CHANGE=true
NO_README_CHANGE=true
HISTORICAL_EVIDENCE_REWRITTEN=false
```
