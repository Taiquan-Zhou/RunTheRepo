# M7.5 DeepSeek portability calibration

## Execution identity

- Code HEAD: `40746267508a3d1583eb58b8f9036f090fab177f`
- Manifest SHA-256: `b050f849802fcf5cb8d035f12d32a063b56b93d080cb580a4ebae852c7b075fd`
- Endpoint class: public OpenAI-compatible endpoint
- Model: `deepseek-v4-flash`

No account identity, credential, request header, prompt, raw response, raw provider error, or token data was retained.

## Calibration result

The existing public planner/evidence path was exercised once with a trusted synthetic README excerpt. The strict request was rejected by the endpoint's structured-output capability and the existing provider-neutral portable JSON path was used exactly once.

| Metric | Result |
|---|---:|
| Strict requests | 1 |
| Portable requests | 1 |
| Policy-valid Journeys | 2 |
| Terminal evidence outcome | `success` |
| Elapsed wall/monotonic time | approximately 58.364 s, including isolated-environment setup |

The portable result remained subject to the existing strict Pydantic full-document validation and planner policy validation. Qwen execution and private Journey transport-schema work were not performed.

## Quality evidence

- `uv run --isolated ruff check .`: PASS.
- `uv run --isolated ruff format --check .`: PASS.
- `uv run --isolated mypy src/repotrial`: PASS (`44` source files).
- `uv run --isolated pre-commit run --all-files`: PASS.
- `git diff --check`: PASS.
- Focused calibration test collection/runtime: PASS (`11 passed`).

Full pytest and coverage both reached the suite and produced `15 failed, 1344 passed, 17 skipped, 4 errors`. A detached pre-adapter `5703f56` worktree, with only the collection repair applied, produced the exact same failure set. The retained failure classes are Windows symlink privilege, a pre-existing lifecycle scheduling assertion, and Windows environment-variable length rejection before RepoTrial domain code runs. New regressions: `0`.

## Scope confirmation

- DockerSbxProvider, Disk Bound, PID policy, whole-trial duration, SandboxProvider architecture, SBX daemon state, manifest entries, and frozen product contracts were not modified.
- No target sandbox or M7.5 repository workload was created or run by this calibration.
