# M7.5 WSL2 compatibility diagnostic canary

## Scope and execution gate

This append-only record covers only Task 2 of the M7.5 compatibility-recovery
plan. The three frozen repositories were executed once each, sequentially, in
the calibrated dedicated WSL2 Ubuntu 24.04 environment through the existing
`DockerSbxProvider`. No daemon lifecycle command, host Docker/Compose fallback,
repository-specific workaround, retry, production/test/manifest change, Task 3
work, push, or merge occurred.

- Execution HEAD: `4565301f1307cfec3de8c97549c978244bce793d`
- WSL checkout: `/home/repotrial/src/RepoTrial`, detached and clean
- Frozen `eval/real_repos.yaml` SHA-256:
  `4a963f0ee730ddf4be7243ad31cc2899ce19cdbfed3c4570498ff7d1c617551a`
- SBX: `v0.39.0` (`def8cb0523a77e757bdd6ef52b459fe374f3783e`)
- `sbx diagnose --output json`: `12` pass, `0` warn, `0` fail, `0` skip
- Initial `sbx list`: exit `0`, exact empty inventory:
  `No sandboxes found.` / `Launch one: sbx run claude`
- All RepoTrial and SBX client commands used the calibrated trusted-client
  environment. Proxy endpoints and values are intentionally not retained here.
- Known limitation retained unchanged: `pid_hard_bound_unsupported`.

All three attempts crossed the controlled Provider boundary when the target
Compose `up` command started, so `target_workload_started=true` under the frozen
attribution rule. Their role and metric attribution are `diagnostic-only`; none
replaces prior history or contributes current release metrics.

## Attempt 1 — Umami

- Run ID: `c4c95907-43a2-465c-b14e-40519ab85134`
- Role / attribution: diagnostic attempt / `diagnostic-only`
- Exact command:

  ```text
  uv run repotrial inspect https://github.com/umami-software/umami --provider docker-sbx --commit-sha ca661c7057984aa98ed4f7083d84dae2f65bfcb0 --container-port 3000 --compose-path docker-compose.yml
  ```

- UTC: `2026-08-30T09:15:24.716878+00:00` to
  `2026-08-30T09:16:07.720770+00:00`; monotonic duration
  `43.00389922699833 s`; timing status `complete`
- Expected SHA = actual verified SHA:
  `ca661c7057984aa98ed4f7083d84dae2f65bfcb0`
- Exit: outer process `1`; retained logical evidence `3`; terminal outcome
  `trial_failed`
- Target workload started: `true`. The controlled Provider boundary started the
  target Compose `up` command; Compose could not open its input, so no target
  service started.
- First failed command / phase: recorded command `up`, baseline Boot / Compose
  up, exit `1`
- Bounded command evidence:
  - `up`: stdout empty, `truncated=false`; stderr
    `open /home/repotrial/src/RepoTrial/artifacts/c4c95907-43a2-465c-b14e-40519ab85134/workspace/docker-compose.yml: no such file or directory\n`,
    `truncated=false`
  - `ps`: stdout empty, `truncated=false`; exit `1`; the same non-truncated
    missing-path stderr
  - `logs`: stdout empty, `truncated=false`; exit `1`; the same non-truncated
    missing-path stderr
- Parsed service states: `{}`; deterministic readiness verdict: `fail`
- Recovery proposal/action: `stop`; bounded rationale: `no recovery action`;
  disposition: `stopped`; final exact stop reason: `boot_recovery_stopped`
- Lifecycle: `create_attempt` -> `create_success` for
  `repotrial-repotrial-baseline-8277a-6c3b8a5843f7` -> `destroy_attempt` ->
  `destroy_success`; derived cleanup: `PASS`
- Post-attempt `sbx list`: exit `0`, exact empty inventory:
  `No sandboxes found.` / `Launch one: sbx run claude`
- Artifacts:
  - `artifacts/c4c95907-43a2-465c-b14e-40519ab85134/attempt-result.json`
  - `artifacts/c4c95907-43a2-465c-b14e-40519ab85134/evidence/baseline-8277a9eb2297e303-0001-attempt-01/baseline-boot-attempt.json`
  - `artifacts/c4c95907-43a2-465c-b14e-40519ab85134/evidence/baseline-8277a9eb2297e303-0001-attempt-01/baseline-lifecycle.jsonl`
  - `artifacts/c4c95907-43a2-465c-b14e-40519ab85134/report/trial-report.{json,html}`

## Attempt 2 — Listmonk

- Run ID: `efd48f46-5c24-4f3d-9c83-4dec02c61003`
- Role / attribution: diagnostic attempt / `diagnostic-only`
- Exact command:

  ```text
  uv run repotrial inspect https://github.com/knadh/listmonk --provider docker-sbx --commit-sha 670c01717d48647093335cc23a6be6f4b79c3b6b --container-port 9000 --compose-path docker-compose.yml
  ```

- UTC: `2026-08-30T09:16:57.288441+00:00` to
  `2026-08-30T09:17:38.426921+00:00`; monotonic duration
  `41.13847722799983 s`; timing status `complete`
- Expected SHA = actual verified SHA:
  `670c01717d48647093335cc23a6be6f4b79c3b6b`
- Exit: outer process `1`; retained logical evidence `3`; terminal outcome
  `trial_failed`
- Target workload started: `true`. The controlled Provider boundary started the
  target Compose `up` command; Compose could not open its input, so no target
  service started.
- First failed command / phase: recorded command `up`, baseline Boot / Compose
  up, exit `1`
- Bounded command evidence:
  - `up`: stdout empty, `truncated=false`; stderr
    `open /home/repotrial/src/RepoTrial/artifacts/efd48f46-5c24-4f3d-9c83-4dec02c61003/workspace/docker-compose.yml: no such file or directory\n`,
    `truncated=false`
  - `ps`: stdout empty, `truncated=false`; exit `1`; the same non-truncated
    missing-path stderr
  - `logs`: stdout empty, `truncated=false`; exit `1`; the same non-truncated
    missing-path stderr
- Parsed service states: `{}`; deterministic readiness verdict: `fail`
- Recovery proposal/action: `stop`; bounded rationale: `no recovery action`;
  disposition: `stopped`; final exact stop reason: `boot_recovery_stopped`
- Lifecycle: `create_attempt` -> `create_success` for
  `repotrial-repotrial-baseline-5c3cc-b03806743ce9` -> `destroy_attempt` ->
  `destroy_success`; derived cleanup: `PASS`
- Post-attempt `sbx list`: exit `0`, exact empty inventory:
  `No sandboxes found.` / `Launch one: sbx run claude`
- Artifacts:
  - `artifacts/efd48f46-5c24-4f3d-9c83-4dec02c61003/attempt-result.json`
  - `artifacts/efd48f46-5c24-4f3d-9c83-4dec02c61003/evidence/baseline-5c3cc71d2ca1daed-0001-attempt-01/baseline-boot-attempt.json`
  - `artifacts/efd48f46-5c24-4f3d-9c83-4dec02c61003/evidence/baseline-5c3cc71d2ca1daed-0001-attempt-01/baseline-lifecycle.jsonl`
  - `artifacts/efd48f46-5c24-4f3d-9c83-4dec02c61003/report/trial-report.{json,html}`

## Attempt 3 — changedetection.io

- Run ID: `e8c3a9df-44c8-4e44-8857-d429ea80e9a3`
- Role / attribution: diagnostic attempt / `diagnostic-only`
- Exact command:

  ```text
  uv run repotrial inspect https://github.com/dgtlmoon/changedetection.io --provider docker-sbx --commit-sha 5d9c7c6da76340597243e8163c4f2439237fa0e8 --container-port 5000 --compose-path docker-compose.yml
  ```

- UTC: `2026-08-30T09:18:09.721221+00:00` to
  `2026-08-30T09:18:51.895250+00:00`; monotonic duration
  `42.17402222200326 s`; timing status `complete`
- Expected SHA = actual verified SHA:
  `5d9c7c6da76340597243e8163c4f2439237fa0e8`
- Exit: outer process `1`; retained logical evidence `3`; terminal outcome
  `trial_failed`
- Target workload started: `true`. The controlled Provider boundary started the
  target Compose `up` command; Compose could not open its input, so no target
  service started.
- First failed command / phase: recorded command `up`, baseline Boot / Compose
  up, exit `1`
- Bounded command evidence:
  - `up`: stdout empty, `truncated=false`; stderr
    `open /home/repotrial/src/RepoTrial/artifacts/e8c3a9df-44c8-4e44-8857-d429ea80e9a3/workspace/docker-compose.yml: no such file or directory\n`,
    `truncated=false`
  - `ps`: stdout empty, `truncated=false`; exit `1`; the same non-truncated
    missing-path stderr
  - `logs`: stdout empty, `truncated=false`; exit `1`; the same non-truncated
    missing-path stderr
- Parsed service states: `{}`; deterministic readiness verdict: `fail`
- Recovery proposal/action: `stop`; bounded rationale: `no recovery action`;
  disposition: `stopped`; final exact stop reason: `boot_recovery_stopped`
- Lifecycle: `create_attempt` -> `create_success` for
  `repotrial-repotrial-baseline-f068e-75ce72bb00ed` -> `destroy_attempt` ->
  `destroy_success`; derived cleanup: `PASS`
- Post-attempt `sbx list`: exit `0`, exact empty inventory:
  `No sandboxes found.` / `Launch one: sbx run claude`
- Artifacts:
  - `artifacts/e8c3a9df-44c8-4e44-8857-d429ea80e9a3/attempt-result.json`
  - `artifacts/e8c3a9df-44c8-4e44-8857-d429ea80e9a3/evidence/baseline-f068ef47424b4ae0-0001-attempt-01/baseline-boot-attempt.json`
  - `artifacts/e8c3a9df-44c8-4e44-8857-d429ea80e9a3/evidence/baseline-f068ef47424b4ae0-0001-attempt-01/baseline-lifecycle.jsonl`
  - `artifacts/e8c3a9df-44c8-4e44-8857-d429ea80e9a3/report/trial-report.{json,html}`

## Dominant-cause ruling

**Ruling: one dominant generic cause exists, with 3/3 qualifying attempts.**

The normalized retained signature is:

```text
phase = baseline Boot / Compose up
command = up
exit = 1
stderr = open <host-ext4-checkout>/artifacts/<run_id>/workspace/docker-compose.yml: no such file or directory
stdout_truncated = false
stderr_truncated = false
```

All three exact-SHA workspaces contain the selected root-relative Compose file
on the WSL host, and all three sandboxes were created successfully. The first
guest Compose command then received a host-side absolute artifact path that was
not present in the sandbox filesystem. It failed after crossing the target
workload execution boundary but before reading Compose input or starting any
target service. This is **Category A — same RepoTrial generic bug**,
specifically a host-to-guest Compose path mapping/command-construction defect.

This conclusion comes from the three non-truncated `up` stderr streams and their
matching command phase/exit, not from the high-level
`boot_recovery_stopped` label. There is no dissenting attempt and no
missing-environment signature. The evidence does not authorize a
repository-specific workaround, a recovery-context/environment correction, or
an inferred readiness correction. Task 3's bounded-recovery/missing-environment
entry gate is therefore not established by this Task 2 ruling; no later task was
started.

## Cleanup and safety conclusion

All `3/3` cleanup obligations passed according to lifecycle JSONL, and every
independent post-attempt `sbx list` returned exact empty inventory. No target
service started, and all three attempts remain `diagnostic-only`. No secrets,
declared environment values, tokens, cookies, device codes, or proxy endpoints
are retained in this document.

## Correction: cloned-workspace allocation and clone postcondition

The preceding path-mapping attribution is retained as the original observed
symptom, but is superseded by later direct 5/64 MiB A/B probes. At 5 MiB the
guest `pwd` was the correct target path, yet the directory was empty: it had no
Git repository and no Compose file despite successful `sbx create`. With only
the cloned-workspace allocation changed to 64 MiB under the same 512 MiB total
policy, Umami, Listmonk, and changedetection.io each had the exact frozen guest
SHA, their selected Compose file, and successful `docker compose config`.

Each probe completed `create_attempt -> create_success -> destroy_attempt ->
destroy_success`, and final inventory was empty. The bounded correction therefore
uses a 1:8 proportional cloned-workspace allocation (with the existing floors)
and verifies the guest clone's top level, work-tree status, frozen HEAD, and
clean status before network allow or ACTIVE state. This is a
workload-conditioned compatibility correction, not an unlimited-repository
claim.

## Post-fix Task 2A diagnostic-only reruns

This append-only section records the three post-fix diagnostic reruns at
code/evidence HEAD `0ae81d8eec376c4af7f89a60c5351d841b28b41c`. It supersedes
only the former correct-path-but-empty-clone symptom: the original attempts and
their earlier path-mapping attribution remain unchanged above. Each rerun used
the frozen repository URL, SHA, Compose path, and port; every actual SHA matched
its pin. The reruns are `diagnostic-only`, are not metric-bearing, and did not
replace any repository.

### Umami

- Run ID: `f4167de9-c652-47be-bcc8-db7190d66bba`; exact SHA:
  `ca661c7057984aa98ed4f7083d84dae2f65bfcb0`; duration:
  `102.2119506329982 s`.
- Commands: `up=1` (stderr truncated with its causal tail retained), `ps=0`,
  `logs=0`. The Compose file was present; images were pulled; containers were
  created and started. The empty-clone/missing-Compose symptom did not recur.
- Final states were `db=restarting` and `umami=created`; the Boot verdict was
  fail. The retained causal signature is PostgreSQL `initdb` repeatedly failing
  to create `/var/lib/postgresql/data/pg_wal` with `No space left on device`.
- Recovery recorded `action=stop`, `disposition=stopped`, reason
  `invalid recovery evidence`, and stop reason `boot_recovery_stopped`.
  Lifecycle recorded `create_success`, `destroy_attempt`, and `destroy_success`.

### Listmonk

- Run ID: `9aca7344-e60a-4d84-ac9a-34c8dfc5b8b9`; exact SHA:
  `670c01717d48647093335cc23a6be6f4b79c3b6b`; duration:
  `60.388527538001654 s`.
- Commands: `up=0`, `ps=0`, `logs=0`, with no stream truncation. The Compose
  file was present; images were pulled; containers were created and started.
  The empty-clone/missing-Compose symptom did not recur.
- Immediate states were `app=restarting` and `db=running/starting`; the Boot
  verdict was fail. Retained logs show PostgreSQL initialized and became ready,
  and, after connection-refused retries, Listmonk completed setup and logged
  `http server started on [::]:9000`. The normalized signature is bounded
  immediate post-up startup/readiness sampling despite later service readiness.
- Recovery recorded `action=stop`, `disposition=stopped`, reason
  `invalid recovery evidence`, and stop reason `boot_recovery_stopped`.
  Lifecycle recorded `create_success`, `destroy_attempt`, and `destroy_success`.

### changedetection.io

- Run ID: `e5211ae6-a2e5-411b-abf6-9038b7b354ba`; exact SHA:
  `5d9c7c6da76340597243e8163c4f2439237fa0e8`; duration:
  `113.89821543700236 s`.
- Commands: `up=0`, `ps=0`, `logs=0`, with no stream truncation. The Compose
  file was present and `changedetection=running`; Boot passed, and retained logs
  show the Flask application listening on port 5000.
- Failure occurred after Boot during Observation collection:
  `exception_type=ObservationCollectionError`, exit code `4`, stop reason
  `internal:observationcollectionerror`, terminal outcome `exception`. The
  retained attempt exposes no more specific collector reason.
- Lifecycle recorded `create_success`, `destroy_attempt`, and `destroy_success`.

After the third rerun, the controller's final `sbx list` recorded the exact
empty inventory `No sandboxes found.` This is a final inventory check, not a
claim that a separate post-attempt list was captured immediately after every
individual rerun.

## Post-fix ruling

The Task 2A cloned-workspace correction is empirically validated `3/3`: all
three reruns reached their exact Compose workload and the old correct-path-but-
empty workspace failure did not recur.

The primary workload outcomes remain heterogeneous: Umami failed Boot with
`up=1` and a hard Docker/workload disk-exhaustion signature; Listmonk failed
Boot with `up=0` after a bounded startup/readiness transition; and
changedetection.io passed Boot before a later `ObservationCollectionError`.

Separately, Umami and Listmonk have the same exact RepoTrial Recovery-phase
failure: each proposal recorded `action=stop`, `disposition=stopped`, and reason
`invalid recovery evidence`. Current code accepts empty `allowed_env_keys` and
an empty README as valid, while `_combined_logs()` rejects fields above `4,096`
characters or aggregate logs above `16,384` characters. Both retained attempts
contain sanitized Boot evidence beyond those bounds. This is the same Recovery
phase and the same normalized planner-input rejection signature in `2/3`.

Therefore the frozen Task 3 entry gate is met for bounded recovery projection.
That authorization does not claim to repair Umami's disk exhaustion or
Listmonk's startup transition; it is limited to repairing the shared RepoTrial
defect that prevents the deterministic planner from receiving valid bounded
evidence. Task 4's readiness entry gate is not met: only Listmonk has the
bounded startup/readiness transition, while Umami has a hard disk error and
changedetection.io passes Boot. Task 5 is not ruled on here, and this section
does not implement Task 3.

**TASK 3 ENTRY GATE MET — BOUNDED RECOVERY PROJECTION AUTHORIZED**

## Post-Task-3 diagnostic-only reruns and Task 4 gate ruling

This append-only section retains the three diagnostic-only reruns completed on
reviewed Task 3 HEAD `922d6357dcb4d782bcaa7a84d5c29fc0f3be4a50`. Focused Task
3 tests passed `117`; `recovery_context` coverage was `86.27%`. The full suite
passed `1233`, with `5` skipped; total coverage was `88.71%`. Ruff check, Ruff
format check, mypy, pre-commit, and `git diff --check` passed. The real SBX
lifecycle smoke passed `1` test in `25.16s`. Every rerun below is
`diagnostic-only`, is not metric-bearing, and does not replace prior history.

### Umami

- Run: `a6f0a1dc-e530-45e8-bec9-4addd307ecc5`.
- Exact expected/actual SHA:
  `ca661c7057984aa98ed4f7083d84dae2f65bfcb0`.
- Duration: `307.7671560730014 s`.
- Attempts 1 and 2: `up=1`; `ps` showed `db=restarting`, `umami=created`;
  logs repeatedly contained PostgreSQL `No space left on device`.
- Recovery for attempts 1 and 2 was schema-valid and applied:
  `action=wait`, reason `recognizable startup delay`. The old
  `invalid recovery evidence` outcome did not recur.
- Attempt 3 was not started as a workload subprocess because the trusted
  host-side deadline was exhausted.
- Terminal stop reason: `sandbox:exec:total_duration_exhausted`.
- All three lifecycle records ended in `destroy_success`.
- Ruling: Task 3 made bounded evidence consumable, but this repository did not
  transition to ready; disk exhaustion and recovery classification remain a
  separate generic issue outside this gate task.

### Listmonk

- Run: `61aa53fd-ca6b-47af-94c5-e0128bf6d8c5`.
- Exact expected/actual SHA:
  `670c01717d48647093335cc23a6be6f4b79c3b6b`.
- Duration: `150.46334362200287 s`.
- First `compose up` ended as `DockerSbxError` before `ps`/`logs`/recovery;
  terminal stop reason `sandbox:exec:timeout`.
- Lifecycle ended in `destroy_success`.
- Ruling: this run is inconclusive for Task 3/Task 4 recovery behavior because
  it never reached a retained startup-transition evidence set.

### changedetection.io

- Run: `4f3a621e-675d-4c77-ad01-b2618e152403`.
- Exact expected/actual SHA:
  `5d9c7c6da76340597243e8163c4f2439237fa0e8`.
- Duration: `157.56752266000694 s`.
- Boot: `up=0`, final service state `changedetection=running`, verdict PASS.
- Later terminal exception remains `ObservationCollectionError`; stop reason
  `internal:observationcollectionerror`.
- Lifecycle ended in `destroy_success`.
- Ruling: this is not a recovery-produced starting/health transition and does
  not satisfy the Task 4 entry-gate signature.

## Task 4 entry gate — NOT APPLICABLE (ENTRY GATE NOT MET)

- `sbx list` after all reruns: `No sandboxes found.`
- Windows and WSL checkouts were clean at exact HEAD `922d635...` before the
  documentation edit.
- Task 4 entry gate requires at least `2/3` latest diagnostics with Compose
  still in a bounded starting/health transition after `up`.
- Observed qualifying count: `0/3`. Umami is a hard disk-exhaustion failure;
  Listmonk timed out before evidence; changedetection passed Boot directly.

Therefore: **Task 4 — NOT APPLICABLE (ENTRY GATE NOT MET)**. No readiness
implementation or calibration is authorized. The known limitation
`pid_hard_bound_unsupported` remains unchanged.

## Final metric-bearing compatibility canary — FAILED

The final execution HEAD was frozen at
`bf62cc72ca3b77e305312f4cc54ff88d49e2bbff`; the manifest SHA-256 remained
`4a963f0ee730ddf4be7243ad31cc2899ce19cdbfed3c4570498ff7d1c617551a`.
Before execution, SBX v0.39.0 diagnose passed `12/12`, the inventory was empty,
and the Windows and WSL tracked worktrees were clean. No tracked file changed
between the three attempts.

| Repo | Final run ID | Exact SHA | Duration | Retained outcome | Cleanup |
|---|---|---|---:|---|---|
| Umami | `b08a3643-4390-4378-b378-a78686ba2ac2` | expected = actual `ca661c7057984aa98ed4f7083d84dae2f65bfcb0` | `309.95240767700307 s` | Attempts 1 and 2 retained `up=1`, `db=restarting`, `umami=created`, PostgreSQL `No space left on device`, and applied `wait` recovery. The shared host deadline then stopped further work; terminal `sandbox:clone_verification:total_duration_exhausted`. No completed report or passing Journey set. | Two created sandboxes ended `destroy_success`; the third create failed; final inventory empty. |
| Listmonk | `cc0cab03-8ca6-41ce-b359-9d5000bc15b5` | expected = actual `670c01717d48647093335cc23a6be6f4b79c3b6b` | `306.3932622449938 s` | Four Boot attempts retained a bounded startup transition and applied `wait`, but the terminal result was `boot_recovery_stopped`. JSON/HTML report completed with `journeys=[]` (`0/0`), so the required non-empty deterministic Journey criterion was not met. | All four created sandboxes ended `destroy_success`; final inventory empty. |
| changedetection.io | `655948c0-d46f-473a-b232-b400768954d5` | expected = actual `5d9c7c6da76340597243e8163c4f2439237fa0e8` | `116.43042066099588 s` | Boot passed with `changedetection=running`, then observation collection raised `ObservationCollectionError`; terminal `internal:observationcollectionerror`. No completed report or passing Journey set. | The created sandbox ended `destroy_success`; final inventory empty. |

The exact canary criterion required exact SHA, a non-empty deterministic
required baseline Journey set, every required Journey passing and persisted, a
completed report, an exact stop reason, successful cleanup, and empty final
inventory. Passing count: `0/3`; required: at least `2/3`. Safety cleanup and
identity evidence passed, but functional/evidence completeness did not.
Repositories #4-#10 were not executed.

**M7.5 COMPATIBILITY CANARY FAILED**

## Task 5 shared readiness implementation observations

Task 5 implementation started from
`73a0212c430ce15a958621acbf72a3110a176580`. No public repository or Pilot was
executed during this task.

- The shared baseline/candidate Boot argv is now exactly
  `docker compose ... up -d --wait --wait-timeout 60`; the Provider command
  timeout passed to this Boot operation remains `120` seconds. The existing
  post-`up` `ps --all --format json` and bounded `logs` calls remain ordered
  after `up`.
- Unit RED observed the previous `up -d` argv across baseline, candidate overlay,
  and environment-prefix cases. The focused Task 5 suite subsequently passed
  `165` tests.
- Generic `starting` evidence no longer produces deterministic `wait` recovery.
  A schema-compatible model-supplied `wait` produced one Boot attempt, no retry,
  private recovery disposition `unsupported`, and exact stop reason
  `boot_recovery_stopped`.
- Repository search found no remaining `RecoverySleep`, `_default_sleep`,
  `GraphContext.sleep`, `context.sleep`, or test `sleep=` injection.
- The fixture evaluator and CLI/integration fakes that consume the shared Boot
  argv were updated mechanically to recognize the new exact suffix. A fresh
  affected-suite run passed `223` tests; its four failures occurred while the
  tests attempted to create Windows symlinks and were the pre-existing
  `WinError 1314` host-permission baseline, before RepoTrial behavior ran.
- An earlier full coverage run reached `85.90%` and collected `1,359` tests,
  with `1,337 passed`, `6 skipped`, and `16 failed`; those failures identified
  stale test consumers of the old argv and were corrected before the fresh
  affected-suite result above. Focused Ruff check/format, mypy across `44`
  source files, and all pre-commit hooks passed in that earlier run; final gates
  remain pending the real SBX smoke.
- Native Windows preflight observed SBX `v0.39.0`, `12/12` diagnose checks
  passing, authentication active, and exact empty `sbx list` inventory.
- The first two pre-restart smoke attempts failed before sandbox/workload
  creation while pulling the official template. The public HTTP `500` was
  traced to sandboxd being unable to connect to its own internal
  `...\\sandboxd\\docker.sock`. After the Owner performed the authorized
  official daemon restart, the daemon process start time changed, diagnose
  returned `12/12` PASS, authentication remained active, and inventory was
  empty. The stale internal-socket failure did not recur.
- Two post-restart diagnostic creates used an intentionally shorter `60`
  second harness command timeout. Sandboxd completed the official template
  pull successfully in `57.315` and `58.103` seconds respectively, but the
  client reached its surrounding 60-second create boundary before sandbox
  creation. Cleanup completed and inventory remained empty. This isolated a
  separate create-timeout calibration issue from Compose readiness.
- The unchanged trusted delayed-health fixture then ran with a `120` second
  create command timeout. It proved create -> `docker compose up -d --wait
  --wait-timeout 60` -> `ps` -> `logs` -> guest `echo ok` -> destroy. Boot
  returned `pass`, normalized service state was `web=running/healthy`, measured
  readiness time was `16.907` seconds, and final native `sbx list` was exactly
  empty.
- Production CLI/API construction currently relies on the Provider's separate
  default create command timeout of `30` seconds. Because the measured official
  template phase alone exceeded that value, this is retained as a generic
  pre-Pilot execution-gate defect to resolve independently; it is not folded
  into the shared-readiness implementation.
