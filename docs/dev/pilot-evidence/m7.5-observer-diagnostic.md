# M7.5 observer compatibility diagnostic

## Frozen execution identity

- Diagnostic HEAD: `b6dc983476abc6eb93dfdc98c9cf5a3b60520cb5`
- Repository: `https://github.com/dgtlmoon/changedetection.io`
- Expected and verified commit:
  `5d9c7c6da76340597243e8163c4f2439237fa0e8`
- Compose path: `docker-compose.yml`
- Container port: `5000`
- Model endpoint: local OpenAI-compatible Ollama endpoint
- Model: `qwen3:4b-instruct`
- Model digest:
  `0edcdef34593eac1aa2be9c7d06c432dcf81945adca5eca2f27662c18f168ba0`
- Frozen manifest SHA-256:
  `b050f849802fcf5cb8d035f12d32a063b56b93d080cb580a4ebae852c7b075fd`

The worktree was clean before the diagnostic. Native SBX v0.39.0 diagnose
passed `12/12`, authentication passed, and the starting inventory was empty.

## Diagnostic attempt

- Run ID: `be31ed36-a99b-40b0-918d-99d8d4186f52`
- Disposition: diagnostic-only; it is not metric-bearing Pilot evidence.
- Duration: `220.95300000000861 s`
- Exact checkout: expected SHA equals actual verified SHA.
- Boot: PASS; the `changedetection` service reached `running`.
- Terminal exception: `ObservationCollectionError`
- Stop reason: `internal:observationcollectionerror`
- Exit code: `4`
- Report disposition: no JSON or HTML report was produced.
- Known limitation remained `pid_hard_bound_unsupported`.
- Lifecycle: `create_attempt -> create_success -> destroy_attempt ->
  destroy_success`.
- Final native `sbx list`: `No sandboxes found.`

The model Journey call separately reached a transport error after `90.469 s`.
That event did not establish the observer failure: the retained observer
boundary evidence independently identifies a collector command failure.

## Exact observer boundary

The private boundary JSONL records these ordered outcomes:

| Collector | Phase | Outcome | Reason | Exit |
|---|---|---|---|---:|
| discovery | parsing | success | collector_succeeded | 0 |
| inspect | parsing | success | collector_succeeded | 0 |
| diff | parsing | success | collector_succeeded | 0 |
| top | result_validation | failure | nonzero_exit | 1 |

The failed command fingerprint belongs to the existing exact argv:

```text
docker top <container-id> -eo pid=,ppid=,user=,comm=
```

This proves that the failure occurs after successful Boot, discovery, inspect,
and diff collection. It is not a high-level inference from
`ObservationCollectionError`.

## Trusted fixture reproduction

A local trusted BusyBox fixture reproduced the same command against the same
healthy SBX daemon. It did not execute a public repository and was destroyed in
a `finally` path. The first harness invocation contained a diagnostic-only
JSON-lines parsing mistake and exited before `docker top`; cleanup completed
and inventory was empty. The corrected harness observed:

| `docker top` arguments | Exit | Result |
|---|---:|---|
| `-eo pid=,ppid=,user=,comm=` | 1 | `Couldn't find PID field in ps output` |
| `-e -o pid=,ppid=,user=,comm=` | 1 | same daemon error |
| `-eo pid,ppid,user,comm` | 0 | standard `PID PPID USER COMMAND` header and four-column row |
| no explicit format | 0 | standard Docker top output |

The final fixture inventory was empty. The controlled A/B result proves a
generic Docker/SBX observer compatibility defect: suppressing the field names
with `=` also suppresses the PID header that this Docker daemon requires to
identify the PID column.

## Correction boundary

The authorized minimal correction is limited to the existing observer, its
focused tests, and exact-command fixture consumers:

- request `pid,ppid,user,comm` without header-suppressing `=`;
- validate and consume exactly one standard four-column header before parsing
  process rows;
- preserve fail-closed behavior for non-zero commands, missing or malformed
  headers, malformed rows, resource limits, evidence recording, and all other
  collectors;
- update the deterministic evaluator and CLI E2E fake to recognize the same
  exact argv and return the same standard header; these are consumers of the
  observer command contract, not alternate runtime behavior;
- make no architecture, provider, manifest, public-contract, PID-policy, Disk
  Bound, or whole-trial-duration change.

## RED/GREEN and review evidence

- Core RED: `25 failed, 99 passed, 1 skipped`. The old production argv and
  headerless parser failed the new exact-format and strict-header tests.
- Core GREEN: `124 passed, 1 skipped`.
- Controller integration RED after the core commit: `15 failed, 167 passed,
  2 skipped`. Twelve failures were the deterministic evaluator rejecting the
  new exact top command; the other three were existing Windows symlink-creation
  privilege failures during test setup.
- After synchronizing the exact-command consumers, the expanded Windows scope
  also included the CLI E2E fixture and was `210 passed, 2 skipped`, with only
  the three `WinError 1314` setup failures.
- The same expanded focused scope under WSL was `214 passed, 1 skipped`.
- Core reviewer ruling for commit `33eaadb173e19461736d535dc7c55c32176f1231`:
  APPROVED, with no Critical, Important, or Minor findings.

The final consumer-sync review, formatting/type gates, and commit identity are
recorded before Task 6 is closed.
