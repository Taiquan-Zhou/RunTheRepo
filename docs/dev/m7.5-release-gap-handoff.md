# RepoTrial M7.5 Release-Gap Handoff

Date: 2026-09-03 (Asia/Shanghai)

This is a factual handoff for the next AI agent. It records the isolated Linux
execution worktree, completed M7.5 tasks, evidence, review state, and safe next
steps. It is not authorization to change production policy or claim release
readiness.

## Superseding continuation checkpoint (2026-09-04, Asia/Shanghai)

This section supersedes older identity/progress statements below while retaining
them as historical evidence.

### Current repository and environment

- Authoritative worktree:
  `/home/repotrial/src/RepoTrial-m7.5-release-gap` in WSL distro
  `RepoTrial-Ubuntu`.
- Branch: `codex/m7.5-release-gap-execution`.
- Current clean HEAD:
  `814c0bfff4cb33daafa20f0cd29ea6d8d79ae400`
  (`fix: validate startup inputs in dynamic sbx roots`).
- No subagent remains open. A Task 7 implementer was stopped before editing;
  Task 7 has no RED, GREEN, real run, or commit yet.
- SBX is `v0.39.0`. Latest diagnosis was `12/12 PASS`; final official inventory
  was exactly empty. Do not issue daemon start/stop/restart/reset commands.
- The Owner enabled Windows proxy TUN plus global routing. WSL reached
  `https://registry-1.docker.io/v2/` and received the expected HTTP `401`.
- Windows user environment contains `REPOTRIAL_MODEL_API_KEY`; never print,
  persist, or place it on a command line. `REPOTRIAL_MODEL_ENDPOINT` is unset;
  the frozen public endpoint used by the canary was
  `https://api.deepseek.com`, model `deepseek-v4-flash`.

### Completed startup-input work

- Implementation chain:
  `15b2c615`, `6a308873`, `6b7e7762`, `0fc1666b`, `0c7c4467`, then Task 4
  commit `814c0bff`.
- The generic feature now plans on the trusted host, materializes a guest-only
  `0600` startup file before Boot, omits closed control keys, validates resolved
  Compose, persists complete terminal evidence, and reuses verified identity
  for baseline/candidate attempts.
- Real SBX revealed that clone roots are dynamic rather than fixed at
  `/workspace`. The final adapter requires absolute normalized non-root
  `pwd -P == git rev-parse --show-toplevel`; host parsing rejects Unicode
  `Cc`/`Cf` controls, and the fixed bind validator rechecks the same root plus
  `realpath` confinement.
- Final trusted fixture: `2 passed in 248.42s`; create -> materialize -> healthy
  Boot -> `echo ok` -> destroy; target mode `0600`; no value leakage; final
  inventory empty.
- Independent Sol-high review: APPROVED, zero Critical/Important findings; both
  Minor findings were resolved before commit.
- Final quality gates: focused `322 passed, 1 skipped`; full
  `1629 passed, 9 skipped, 1 warning`; branch coverage `87.13%`; Ruff,
  format, mypy, pre-commit, diff-check, and secret-pattern scan PASS.

### Frozen Linkding result

- Sole valid run: `8f4b9f78-e1de-46d6-b1c2-98ffa464c232`.
- Requested exact SHA:
  `65813a75404b1319aca8b09700fadc0b15adabaf`.
- Terminal result: exit `4`, `RepoIntakeError`,
  `stop_reason=intake:clone_timeout`, duration `125.083775s`.
- `actual_verified_sha=null`; checkout, sandbox creation, workload, startup
  materialization, Boot, Journey, experiment, and report were not reached.
- Final inventory was empty. Do not rerun or count Linkding as recovered.
  Generic startup-input materialization is proven by the trusted fixture, but
  Linkding recovery remains unproven and recovery-count increment is zero.

### Exact next task

Continue at Task 7, `RG3 Optional Guest-Loopback Diagnosis Only`, in
`docs/superpowers/plans/2026-09-02-m7.5-release-gap-closure.md`.

1. RED/GREEN only these new paths:
   `tests/integration/sandbox/fixtures/loopback_publication/**` and
   `tests/integration/sandbox/test_loopback_publication_diagnostic.py`.
2. Use pinned unprivileged BusyBox and guest Compose publication
   `127.0.0.1:8080:8080`; observe in order container-local, guest-loopback, and
   `SandboxProvider.publish_port()` reachability. Persist only bounded address
   class, port, status, and error evidence.
3. Run the trusted fixture with `REPOTRIAL_RUN_REAL_SBX=1`, one sandbox at a
   time, using `disk_mb=8192`, managed cleanup, and exact empty inventory before
   and after.
4. Run the same three probes once for changedetection.io exact SHA
   `5d9c7c6da76340597243e8163c4f2439237fa0e8`, Compose
   `docker-compose.yml`, port `5000`; preserve the result.
5. This task is diagnosis-only. Do not implement a compatibility overlay or
   modify production, graph/state, Provider, manifest, README, Journey,
   mutation, or KEEP/ROLLBACK semantics. Even if the RG3 gate passes, stop for
   a separate design and Owner approval.
6. After independent review, commit Task 7 evidence, then execute Task 8 to
   mechanically recompute the same-HEAD recovery count. If it remains below
   four, record `STAGE1_RECOVERY_GATE_UNMET`; do not run the full cohort.

All detailed hashes and append-only facts are in
`docs/dev/pilot-evidence/m7.5-release-gap-closure.md` and `docs/dev/status.md`.

## Current identity

- Authoritative worktree: `/home/repotrial/src/RepoTrial-m7.5-release-gap` in
  WSL distro `RepoTrial-Ubuntu`.
- Branch: `codex/m7.5-release-gap-execution`.
- HEAD: `7d23c201ca3bf1c77a334a2b0fc94ea4a8ff4f1c`.
- Worktree was clean at handoff.
- Windows checkout `D:\A all code\RepoTrial` is not the authoritative Linux
  execution checkout.
- Docker Sandboxes daemon was healthy and `sbx list` was empty after each run.

For future SBX commands, explicitly set `HTTP_PROXY`, `HTTPS_PROXY`, and
`ALL_PROXY` to the already verified LAN proxy `http://172.24.64.1:7890`, with
`NO_PROXY=localhost,127.0.0.1`. Do not commit these values or record secrets.

## Governing files and constraints

- Plan: `docs/superpowers/plans/2026-09-02-m7.5-release-gap-closure.md`
- Task 2 brief: `.superpowers/sdd/2026-09-02-m7.5-release-gap-closure/task-2-brief.md`
- Progress ledger: `.superpowers/sdd/2026-09-02-m7.5-release-gap-closure/progress.md`
- Evidence ledger: `docs/dev/pilot-evidence/m7.5-release-gap-closure.md`
- Status summary: `docs/dev/status.md`

The plan requires serial work, disposable `DockerSbxProvider` isolation, exact
frozen repository SHAs/Compose paths, deterministic verifiers, append-only
evidence, and separate Owner-approved designs before production corrections.
Unknown observability must remain `not_observed` or `unsupported`. Never run
target Compose on the real host, expose a host Docker socket, or expose real
credentials, SSH keys, cloud tokens, metadata, or LAN access. Do not rewrite
historical M7.5 evidence.

## Task 1 result

Task 1 re-established the Linux baseline and identified the combined SBX test
failure. The WSL test process did not inherit the Docker Hub proxy. Docker Hub
credential refresh and refresh-lock waits then consumed trial duration. Without
the explicit proxy, trusted calibration returned `2 failed, 1 passed`; with the
proxy it returned `3 passed in 270.40s`.

Relevant commits are `5c82659` (plan formatting), `ebaa984` (Task 1 final gate),
and `2367ba4` (accepted Task 1 HEAD). Locked uv checks, Ruff lint/format, mypy,
and full pytest (`1519 passed, 6 skipped, 1 warning`) passed at the corrected
HEAD. No production code changed. Independent reviewer dispatches returned
external HTTP 502/503; no reviewer approval was claimed.

## Task 2 scope and runner

Task 2 was evidence-only. Allowed changes were the append-only evidence ledger,
`docs/dev/status.md`, and the bounded runner
`docs/dev/pilot-evidence/scripts/m7.5_disk_probe.py`. Production code, policy
defaults, public API/CLI options, manifest, README, and target repositories
were not changed.

Runner SHA-256:
`967970ba7f58af4c96b6f4775857e06ca812a9495dd434574d8c19ee5674274f`

The runner accepts only disk totals `4096` or `8192`, constructs
`cpus=1,memory_mb=1024,pids_limit=64,disk_mb=<selected>,total_duration_s=900`,
injects `DockerSbxProvider` through `provider_factory`, and invokes the existing
Typer `inspect` command. Model name is `deepseek-v4-flash`; no key or secret
endpoint value was recorded.

The unchanged allocation function yields `2048 -> root 1 / Docker 1919 /
workspace 128 MiB`, `4096 -> 1 / 3839 / 256 MiB`, and `8192 -> 1 / 7679 /
512 MiB`. Normal CLI/API defaults remain `disk_mb=2048`.

## RG1 run evidence

Runs were strictly serial, with empty inventory before each dequeue and after
cleanup. The complete audit index is in the evidence ledger.

At 4096 MiB, Uptime Kuma (`a852e21eba4ecf339624b404518c5bc7fad6d45c`,
`compose.yaml`, port 3001) produced run
`9a41bd43-16e7-41b6-a55a-f8dc639ac3de`, UTC
`05:17:19.408139Z -> 05:20:26.134180Z`. Boot/health succeeded but Journey
coverage was insufficient, exit 3, terminal `trial_failed`; cleanup succeeded.

At 4096 MiB, n8n (`6b78193475d84ae190622d8a8e9ba8598e89b7d1`,
`docker-compose/withPostgres/docker-compose.yml`, port 5678) produced run
`0dbec259-89aa-4fc5-955b-94fb232dd4b7`, UTC
`05:22:51.055325Z -> 05:26:13.049441Z`. Image extraction failed with
`no space left on device`, terminal `boot_recovery_stopped`, exit 3; cleanup
succeeded.

At 8192 MiB, Uptime Kuma produced run
`db7dcfa3-64be-4e3c-b86e-416486fb5e2a`, UTC
`05:41:46.064066Z -> 05:45:21.541748Z`; boot/health succeeded but Journey
coverage was insufficient, exit 3, terminal `trial_failed`; cleanup succeeded.

At 8192 MiB, n8n produced run
`8bbd34be-af44-4c3a-b500-f3446c11ce70`, UTC
`05:46:30.057268Z -> 05:51:01.134906Z`; it ended with `DockerSbxError`,
`sandbox:exec:timeout`, exit 4, with no report; cleanup succeeded.

Admission was `/dev/sdd`, total 120380 MiB, available 106783 MiB, state usage
2234 MiB, empty inventory. After the queue, available was 106740 MiB and usage
2237 MiB. The conservative reserve calculation was `2234 + 8192 = 10426 MiB`.
Separate fixed SBX overhead, live peak state-volume samples, and guest `df`
were not exposed by retained artifacts and are explicitly `not_observed`.

## Gate conclusion and meaning of 2048

Authoritative ruling: `RG1_CAPACITY_GATE=FAIL no_common_bounded_total_through_8192`.

This does not mean the WSL host disk is full; the host had over 100 GiB free.
`2048 MiB` is the per-sandbox total disk budget, divided into Docker-data,
workspace, and root allocations. n8n exhausted Docker-data at 4096 MiB; at
8192 MiB it timed out without a completed report. There is therefore no evidence
that changing the default to 8192 would solve the problem.

Consequences: keep the current 2048 MiB default; do not create
`2026-09-02-m7.5-rg1-policy.md`; do not run a policy canary or claim release
readiness. Any timeout/observability investigation or production correction
needs a new bounded design, independent review, and Owner approval.

## Commits and review state

Task 2 commits: `032e4d9` (runner and initial evidence), `b4b5366` (status),
`813733a` (per-run evidence field completion), and `7d23c20` (status link).

An independent reviewer reviewed the earlier `032e4d9` range and returned
`CHANGES REQUESTED` for missing status and per-run fields. Those findings were
addressed by the later commits with exact UTC intervals, argv, allocations,
cleanup/lifecycle results, numeric reserve calculation, and explicit unknown
markers. A fresh verdict on `7d23c20` was not obtained before handoff; do not
call Task 2 independently approved. Three implementer dispatches using
`gpt-5.6-sol` failed before execution with relay HTTP 503. This was recorded as
a process deviation and is not a production-code failure.

## Safe next-AI procedure

1. Read `AGENTS.md`, this handoff, the plan, Task 2 brief, evidence ledger, and
   progress ledger.
2. Confirm the WSL worktree, branch, HEAD, and clean status.
3. Run read-only `sbx diagnose`, `sbx list`, and `git diff 2367ba4..HEAD --check`
   with the proxy environment above.
4. Obtain a fresh independent review of the full Task 2 diff if the relay is
   available. If it fails, record the failure; never invent approval.
5. Do not automatically begin Task 3 or the remaining frozen cohort. RG1 FAIL
   does not authorize a policy change. Preserve all historical evidence
   append-only.

## Final statement

The completed work proves two bounded facts: missing WSL proxy inheritance caused
the earlier combined calibration failure, and RG1 did not prove a common
Docker-data capacity through 8192 MiB. No RepoTrial production behavior,
default policy, public interface, manifest, README, or target repository was
modified.

## Current-state correction (append-only; 2026-09-03)

The current RG1 state supersedes the earlier gate interpretation while
preserving every historical paragraph above. Independent review of the raw
evidence confirms that 4096 Uptime Kuma crossed the capacity boundary, 4096
n8n hit Docker-data `ENOSPC`, and both Uptime Kuma and n8n crossed image
pull/extraction at 8192. n8n `Boot up`, `ps`, and `logs` each exited `0`.

The subsequent n8n `sandbox:exec:timeout` was a post-Boot observation failure
from `docker diff`; it cannot negate the capacity result. The overall n8n
trial still failed and must not be counted as autonomous success. The final
capacity verdict is:

```text
RG1_CAPACITY_GATE=PASS common_total_mib=8192
DISK DEFAULT CHANGE PROPOSED - OWNER DECISION REQUIRED
current_default_mib=2048
proven_candidate_mib=8192
state_volume_required_mib=10426
maximum_supported_concurrent_target_sandboxes=1
choices=retain-2048 | approve-global-proven-candidate | retain-2048-and-request-separate-per-run-design
```

`state_volume_required_mib=10426` is a conservative admission/reserve upper
bound (`2234 MiB` measured state baseline plus `8192 MiB` selected total), not
a live peak. Live peak state-volume usage, guest `df`, and independent fixed
SBX overhead remain `not_observed`.

Task 1 independent review is `content APPROVE`, with its historical
review-service/process deviation retained. Task 2's original review verdict
was `CHANGES REQUESTED`; this submission fixes the erroneous interpretation
only and still requires independent review. No RG1 policy plan is created and
no CLI/API default is changed; Owner choice remains required.
